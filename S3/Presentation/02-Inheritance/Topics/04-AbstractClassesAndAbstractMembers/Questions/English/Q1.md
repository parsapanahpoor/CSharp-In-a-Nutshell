<div dir="ltr" style="text-align: left;">

## ❓ Q1:

## What is Abstract Class?

### ✅ Answer:

**Abstract Class** is a class that cannot be **directly instantiated**. It's only used for inheritance.

<div dir="ltr" style="text-align: left;">

```csharp
abstract class Animal {
    public string Name { get; set; }
}

// ❌ Cannot do this:
// Animal animal = new Animal();  // Error!

// ✅ But we can use Derived:
class Dog : Animal { }
Dog dog = new Dog();  // ✅ OK
```

<div dir="ltr" style="text-align: left;">

### 🎯 Note:

Abstract Class is a **Template** or **Base Contract** that derived classes must follow.
