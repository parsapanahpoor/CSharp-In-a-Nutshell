<div dir="ltr" style="text-align: left;">

## ❓ Q8:

## Can Abstract Class have Constructor?

### ✅ Answer:

Yes! Abstract Class can have Constructor, but it cannot be called directly.

<div dir="ltr" style="text-align: left;">

```csharp
abstract class Animal {
    protected string name;
    
    // ✅ Can have constructor:
    protected Animal(string name) {
        this.name = name;
    }
}

class Dog : Animal {
    public Dog(string name) : base(name) {  // ✅ Use base constructor
        // ...
    }
}

// ❌ Cannot call directly:
// Animal animal = new Animal("Test");  // Error!
```

<div dir="ltr" style="text-align: left;">

### 🎯 Note:

Abstract Class Constructor is usually **protected** so it's only accessible from derived classes.
