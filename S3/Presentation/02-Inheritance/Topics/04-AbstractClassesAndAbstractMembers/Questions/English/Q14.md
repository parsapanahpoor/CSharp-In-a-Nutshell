<div dir="ltr" style="text-align: left;">

## ❓ Q14:

## How do Abstract Class work with Fields?

### ✅ Answer:

Abstract Class can have **Fields** (unlike Interface).

<div dir="ltr" style="text-align: left;">

```csharp
abstract class Animal {
    protected string name;  // ✅ Field
    protected int age;       // ✅ Field
    
    protected Animal(string name, int age) {
        this.name = name;
        this.age = age;
    }
    
    public abstract void Speak();
    
    public void DisplayInfo() {
        Console.WriteLine($"{name} is {age} years old");
    }
}

class Dog : Animal {
    public Dog(string name, int age) : base(name, age) { }
    
    public override void Speak() {
        Console.WriteLine($"{name} says: Woof!");
    }
}
```

<div dir="ltr" style="text-align: left;">

### 🎯 Note:

Fields are one of the advantages of Abstract Class over Interface. We can maintain **State** in Abstract Class.
