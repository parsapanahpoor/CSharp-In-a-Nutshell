<div dir="ltr" style="text-align: left;">

## ❓ Q7:

## Are Abstract Methods implicitly virtual?

### ✅ Answer:

Yes! Abstract Methods are **implicitly virtual**. We cannot write `virtual abstract`.

<div dir="ltr" style="text-align: left;">

```csharp
abstract class Animal {
    // These two are the same:
    public abstract void Method1();  // ✅ Implicitly virtual
    // public virtual abstract void Method1();  // ❌ Error! Cannot write virtual
}
```

<div dir="ltr" style="text-align: left;">

### 🎯 Note:

Abstract Methods are **always virtual** because they must be overridden. No need to write `virtual`.
