<div dir="rtl" style="text-align: right;">

## ❓ Q19:

## Abstract Class با Reflection و Runtime Type Information چطور کار می‌کنه؟

### ✅ جواب عمیق:

Reflection می‌تونه Abstract Classes و Abstract Methods رو در runtime بررسی کنه.

### 🔍 Reflection برای Abstract Classes:

<div dir="ltr" style="text-align: left;">

```csharp
abstract class Animal {
    public abstract void MakeSound();
    public void Sleep() { }
}

class Dog : Animal {
    public override void MakeSound() { }
}

// Reflection:
Type animalType = typeof(Animal);
Type dogType = typeof(Dog);

// بررسی Abstract Class:
bool isAbstract = animalType.IsAbstract;  // True ✅
bool canInstantiate = !animalType.IsAbstract;  // False

// بررسی Abstract Methods:
MethodInfo[] methods = animalType.GetMethods();
foreach (var method in methods) {
    if (method.IsAbstract) {
        Console.WriteLine($"Abstract: {method.Name}");  // MakeSound ✅
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🔬 Abstract Method Invocation:

<div dir="ltr" style="text-align: left;">

```csharp
Animal animal = new Dog();

// با Reflection می‌تونیم abstract method رو صدا بزنیم:
MethodInfo method = typeof(Animal).GetMethod("MakeSound");
method.Invoke(animal, null);  // ✅ Virtual dispatch کار می‌کنه!

// یا مستقیم:
animal.MakeSound();  // ✅ Virtual call → Dog.MakeSound()
```

<div dir="rtl" style="text-align: right;">

### 🔍 Type Information:

<div dir="ltr" style="text-align: left;">

```csharp
// بررسی اینکه آیا کلاس همه abstract methods رو implement کرده:
Type type = typeof(Dog);
Type baseType = type.BaseType;  // Animal

MethodInfo[] abstractMethods = baseType.GetMethods()
    .Where(m => m.IsAbstract)
    .ToArray();

MethodInfo[] implementedMethods = type.GetMethods()
    .Where(m => m.GetBaseDefinition() != m)  // Override شده
    .ToArray();

bool allImplemented = abstractMethods.All(am => 
    implementedMethods.Any(im => im.GetBaseDefinition() == am)
);
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته پیشرفته:

Reflection می‌تونه **Abstract Class Metadata** رو بخونه و بررسی کنه که آیا کلاس مشتق همه abstract methods رو implement کرده یا نه.

### ⚠️ Performance:

Reflection برای Abstract Classes **خیلی کند** است (~1000-10000 CPU cycles). فقط برای **Inspection** استفاده کن، نه برای **Invocation**!

### 💡 استفاده:

- **Plugin Systems**: بررسی اینکه آیا کلاس همه abstract methods رو implement کرده
- **Code Analysis**: بررسی Abstract Class hierarchies
- **Testing**: بررسی Abstract Class contracts

