<div dir="rtl" style="text-align: right;">

## ❓ Q18:

## Abstract Class Versioning و Breaking Changes چطور مدیریت می‌شن؟

### ✅ جواب عمیق:

Abstract Classes در Versioning **مشکلاتی** دارن که باید دقت کنیم.

### ⚠️ مشکل Versioning:

<div dir="ltr" style="text-align: left;">

```csharp
// Version 1:
abstract class Animal {
    public abstract void MakeSound();
}

class Dog : Animal {
    public override void MakeSound() { }
}

// Version 2: یک abstract method جدید اضافه می‌کنیم
abstract class Animal {
    public abstract void MakeSound();
    public abstract void Move();  // ⚠️ Breaking Change!
}

// حالا همه کلاس‌های مشتق باید Move() رو implement کنن:
class Dog : Animal {
    public override void MakeSound() { }
    public override void Move() { }  // ✅ باید اضافه کنیم
}
```

<div dir="rtl" style="text-align: right;">

### ✅ راه حل‌ها:

**1. Virtual Methods به جای Abstract:**

<div dir="ltr" style="text-align: left;">

```csharp
abstract class Animal {
    public abstract void MakeSound();
    
    // به جای abstract، virtual با default implementation:
    public virtual void Move() {
        Console.WriteLine("Default movement");  // ✅ Default behavior
    }
}
```

<div dir="rtl" style="text-align: right;">

**2. Extension Methods:**

<div dir="ltr" style="text-align: left;">

```csharp
abstract class Animal {
    public abstract void MakeSound();
}

// Extension method (بدون breaking change):
static class AnimalExtensions {
    public static void Move(this Animal animal) {
        Console.WriteLine("Default movement");
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🔬 Interface Segregation:

<div dir="ltr" style="text-align: left;">

```csharp
// به جای یک Abstract Class بزرگ:
interface IAnimal {
    void MakeSound();
    void Move();
    void Eat();
}

// از Interface Segregation استفاده کن:
interface IAnimal {
    void MakeSound();
}

interface IMovable {
    void Move();
}

interface IFeedable {
    void Eat();
}

// کلاس‌ها فقط interface های مورد نیاز رو implement می‌کنن
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته پیشرفته:

**Abstract Classes** برای Versioning **سخت‌تر** از Interfaces هستند چون:
- اضافه کردن abstract method = Breaking Change
- نمی‌تونیم Multiple Inheritance داشته باشیم
- کلاس‌های مشتق باید همه abstract methods رو implement کنن

### 💡 توصیه:

برای **Public APIs**، از **Interfaces** استفاده کن. Abstract Classes برای **Internal/Private** hierarchies بهترن.

