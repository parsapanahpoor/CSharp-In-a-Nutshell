<div dir="rtl" style="text-align: right;">

## ❓ Q8:

## Abstract Class می‌تونه Constructor داشته باشه؟

### ✅ جواب:

بله! Abstract Class می‌تونه Constructor داشته باشه، اما نمی‌تونه مستقیماً صدا زده بشه.

<div dir="ltr" style="text-align: left;">

```csharp
abstract class Animal {
    protected string name;
    
    // ✅ می‌تونیم constructor داشته باشیم:
    protected Animal(string name) {
        this.name = name;
    }
}

class Dog : Animal {
    public Dog(string name) : base(name) {  // ✅ از base constructor استفاده می‌کنیم
        // ...
    }
}

// ❌ نمی‌تونیم مستقیماً صدا بزنیم:
// Animal animal = new Animal("Test");  // Error!
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

Constructor Abstract Class معمولاً **protected** است تا فقط از کلاس‌های مشتق قابل دسترسی باشه.

