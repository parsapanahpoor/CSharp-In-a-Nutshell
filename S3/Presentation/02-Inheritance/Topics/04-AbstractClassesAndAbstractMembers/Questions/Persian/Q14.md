<div dir="rtl" style="text-align: right;">

## ❓ Q14:

## Abstract Class با Fields چطور کار می‌کنه؟

### ✅ جواب:

Abstract Class می‌تونه **Fields** داشته باشه (برخلاف Interface).

<div dir="ltr" style="text-align: left;">

```csharp
abstract class Animal {
    protected string name;  // ✅ Field
    protected int age;       // ✅ Field
    
    protected Animal(string name, int age) {
        this.name = name;
        this.age = age;
    }
    
    public abstract void Speak();
    
    public void DisplayInfo() {
        Console.WriteLine($"{name} is {age} years old");
    }
}

class Dog : Animal {
    public Dog(string name, int age) : base(name, age) { }
    
    public override void Speak() {
        Console.WriteLine($"{name} says: Woof!");
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

Fields یکی از مزایای Abstract Class نسبت به Interface است. می‌تونیم **State** رو در Abstract Class نگه داریم.

