<div dir="rtl" style="text-align: right;">

## ❓ Q20:

## Abstract Class با Design Patterns و SOLID Principles چطور کار می‌کنن؟

### ✅ جواب عمیق:

Abstract Classes در Design Patterns و SOLID Principles نقش مهمی دارن.

### 🔍 Template Method Pattern:

<div dir="ltr" style="text-align: left;">

```csharp
// Template Method Pattern با Abstract Class:
abstract class DataProcessor {
    // Template Method (Concrete):
    public void Process() {
        LoadData();      // Abstract
        ValidateData();  // Virtual
        TransformData(); // Abstract
        SaveData();      // Concrete
    }
    
    protected abstract void LoadData();
    protected virtual void ValidateData() {
        Console.WriteLine("Default validation");
    }
    protected abstract void TransformData();
    protected void SaveData() {
        Console.WriteLine("Saving...");
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🔬 SOLID Principles:

**1. Single Responsibility Principle (SRP):**

<div dir="ltr" style="text-align: left;">

```csharp
// ❌ بد: یک Abstract Class با مسئولیت‌های زیاد
abstract class Animal {
    public abstract void MakeSound();
    public abstract void Move();
    public abstract void Eat();
    public abstract void Reproduce();
    // خیلی مسئولیت!
}

// ✅ خوب: Interface Segregation
interface IAnimal {
    void MakeSound();
}
interface IMovable {
    void Move();
}
interface IFeedable {
    void Eat();
}
```

<div dir="rtl" style="text-align: right;">

**2. Open/Closed Principle (OCP):**

<div dir="ltr" style="text-align: left;">

```csharp
// ✅ Abstract Class برای Extension:
abstract class Shape {
    public abstract double Area { get; }
    public virtual void Draw() {
        Console.WriteLine("Drawing shape");
    }
}

// می‌تونیم انواع جدید اضافه کنیم بدون تغییر Shape:
class Circle : Shape {
    public override double Area => Math.PI * radius * radius;
}
```

<div dir="rtl" style="text-align: right;">

**3. Liskov Substitution Principle (LSP):**

<div dir="ltr" style="text-align: left;">

```csharp
// ✅ Abstract Class باید LSP رو رعایت کنه:
abstract class Animal {
    public abstract void Move();
}

class Dog : Animal {
    public override void Move() {
        Console.WriteLine("Running");  // ✅ رفتار منطقی
    }
}

// ❌ نقض LSP:
class Fish : Animal {
    public override void Move() {
        throw new NotSupportedException("Fish can't move on land!");
        // ❌ این نقض LSP است!
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🔬 Dependency Inversion Principle (DIP):

<div dir="ltr" style="text-align: left;">

```csharp
// ✅ Abstract Class برای Dependency Inversion:
abstract class DataRepository {
    public abstract void Save(object data);
    public abstract object Load(int id);
}

class SqlRepository : DataRepository {
    public override void Save(object data) { }
    public override object Load(int id) { }
}

// Client code به Abstract Class وابسته است، نه Concrete:
class DataService {
    private DataRepository repository;  // ✅ Abstract dependency
    
    public DataService(DataRepository repo) {
        repository = repo;
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته پیشرفته:

Abstract Classes برای **Template Method Pattern** و **Dependency Inversion** عالی هستند، اما باید **LSP** و **SRP** رو رعایت کنن.

### ⚠️ Trade-offs:

- **Abstract Classes**: Partial Implementation، اما Single Inheritance
- **Interfaces**: Multiple Inheritance، اما بدون Implementation

### 💡 توصیه:

از **Abstract Classes** برای **Internal Hierarchies** و **Template Methods** استفاده کن.
از **Interfaces** برای **Public Contracts** و **Multiple Inheritance** استفاده کن.

