<div dir="rtl" style="text-align: right;">

## ❓ Q16:

## Abstract Class Instantiation و Memory Layout چطور کار می‌کنه؟

### ✅ جواب عمیق:

Abstract Class نمی‌تونه **مستقیماً** instantiate بشه، اما می‌تونه از طریق **Derived Classes** instantiate بشه.

### 🔍 Memory Layout:

<div dir="ltr" style="text-align: left;">

```csharp
abstract class Animal {
    protected string name;
    public abstract void Speak();
    public void Sleep() { }
}

class Dog : Animal {
    private int age;
    public override void Speak() { }
}
```

<div dir="rtl" style="text-align: right;">

### 📊 Memory Structure:

<div dir="ltr" style="text-align: left;">

```
Dog Object in Heap:
┌─────────────────────────┐
│ Object Header           │
├─────────────────────────┤
│ Type Handle (Dog)       │ → Points to Dog Type Info
├─────────────────────────┤
│ Animal.name (string)    │ → Base class field
├─────────────────────────┤
│ Dog.age (int)           │ → Derived class field
└─────────────────────────┘
```

<div dir="rtl" style="text-align: right;">

### ⚙️ Instantiation Process:

<div dir="ltr" style="text-align: left;">

```csharp
Dog dog = new Dog();

// چه اتفاقی می‌افته:
// 1. Memory برای Dog object allocate می‌شه
// 2. Base class fields (Animal.name) initialize می‌شن
// 3. Derived class fields (Dog.age) initialize می‌شن
// 4. Base constructor صدا زده می‌شه
// 5. Derived constructor صدا زده می‌شه
// 6. VMT برای Dog ساخته می‌شه (با override methods)
```

<div dir="rtl" style="text-align: right;">

### 🔬 VMT برای Abstract Methods:

<div dir="ltr" style="text-align: left;">

```
Dog VMT:
┌─────────────────────┐
│ [0] Dog.Speak()     │ → Override شده از Animal
│ [1] Animal.Sleep()  │ → Inherited از Animal
└─────────────────────┘
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته پیشرفته:

Abstract Methods در VMT **ندارن**! فقط override شده‌ها در VMT هستند. Abstract Methods فقط در **Metadata** ذخیره می‌شن.

### ⚠️ مهم:

Abstract Class **نمی‌تونه** مستقیماً instantiate بشه چون **incomplete** است. باید همه abstract methods implement بشن.

