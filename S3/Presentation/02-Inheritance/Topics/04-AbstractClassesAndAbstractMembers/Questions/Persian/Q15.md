<div dir="rtl" style="text-align: right;">

## ❓ Q15:

## Abstract Methods با Events چطور کار می‌کنن؟

### ✅ جواب:

Events نمی‌تونن abstract باشن، اما می‌تونیم از **Abstract Methods** برای raise کردن events استفاده کنیم.

<div dir="ltr" style="text-align: left;">

```csharp
abstract class Animal {
    public event EventHandler? Spoke;
    
    protected abstract void OnSpoke();  // ✅ Abstract method
    
    public void Speak() {
        Console.WriteLine("Speaking...");
        OnSpoke();  // ✅ Abstract method صدا زده می‌شه
    }
}

class Dog : Animal {
    protected override void OnSpoke() {  // ✅ باید implement کنیم
        Spoke?.Invoke(this, EventArgs.Empty);
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

این pattern برای **Template Method** در Event Raising استفاده می‌شه. کلاس مشتق باید OnSpoke رو implement کنه.

