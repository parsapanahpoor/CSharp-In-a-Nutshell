<div dir="rtl" style="text-align: right;">

## ❓ Q10:

## چه موقع از Abstract Class استفاده کنیم؟

### ✅ جواب:

وقتی می‌خوایم **Partial Implementation** داشته باشیم و کلاس‌های مشتق باید بعضی متدها رو implement کنن.

<div dir="ltr" style="text-align: left;">

```csharp
// مثال: Template Method Pattern
abstract class DataProcessor {
    // Template Method (Concrete):
    public void Process() {
        LoadData();      // Abstract
        ValidateData();  // Virtual (می‌تونه override بشه)
        TransformData(); // Abstract
        SaveData();      // Concrete
    }
    
    protected abstract void LoadData();
    protected virtual void ValidateData() {
        Console.WriteLine("Default validation");
    }
    protected abstract void TransformData();
    protected void SaveData() {
        Console.WriteLine("Saving...");
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 استفاده‌ها:

1. **Template Method Pattern**
2. وقتی می‌خوایم **Common Code** رو share کنیم
3. وقتی می‌خوایم **Contract** تعریف کنیم اما **Partial Implementation** هم داشته باشیم

### ⚠️ نکته:

اگر فقط Contract می‌خوای (بدون implementation)، از **Interface** استفاده کن!

