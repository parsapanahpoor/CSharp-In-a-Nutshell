<div dir="rtl" style="text-align: right;">

## ❓ Q3:

## تفاوت Abstract Class با Interface چیه؟

### ✅ مقایسه:

| ویژگی | Abstract Class | Interface |
|-------|---------------|-----------|
| **Implementation** | می‌تونه داشته باشه | نمی‌تونه داشته باشه |
| **Fields** | می‌تونه داشته باشه | نمی‌تونه داشته باشه |
| **Constructor** | می‌تونه داشته باشه | نمی‌تونه داشته باشه |
| **Multiple Inheritance** | ❌ فقط یک کلاس | ✅ چند interface |
| **Access Modifier** | می‌تونه protected باشه | همیشه public |

<div dir="ltr" style="text-align: left;">

```csharp
// Abstract Class:
abstract class Animal {
    protected string name;  // ✅ Field
    public Animal(string n) { name = n; }  // ✅ Constructor
    public void Sleep() { }  // ✅ Concrete method
    public abstract void Speak();  // ✅ Abstract method
}

// Interface:
interface IAnimal {
    // string name;  // ❌ نمی‌تونه field داشته باشه
    // IAnimal() { }  // ❌ نمی‌تونه constructor داشته باشه
    void Speak();  // ✅ فقط method signature
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

**Abstract Class** = Partial Implementation | **Interface** = Pure Contract

