<div dir="rtl" style="text-align: right;">

## ❓ Q17:

## Abstract Class با Generic Constraints و Type Parameters چطور کار می‌کنه؟

### ✅ جواب عمیق:

Abstract Generic Classes می‌تونن خیلی پیچیده بشن، مخصوصاً وقتی با Type Constraints ترکیب می‌شن.

### 🔍 مثال پیچیده:

<div dir="ltr" style="text-align: left;">

```csharp
abstract class Container<T> where T : Animal {
    protected T item;
    
    public Container(T item) {
        this.item = item;
    }
    
    public abstract void Process();  // ✅ Abstract method
    
    public virtual void Display() {
        Console.WriteLine($"Container: {typeof(T).Name}");
    }
}

class DogContainer : Container<Dog> {
    public DogContainer(Dog dog) : base(dog) { }
    
    public override void Process() {
        item.Bark();  // ✅ می‌دونه T = Dog است
    }
}
```

<div dir="rtl" style="text-align: right;">

### ⚠️ محدودیت‌های Abstract Generic:

<div dir="ltr" style="text-align: left;">

```csharp
// ❌ نمی‌تونیم این کارها رو بکنیم:

// 1. Abstract method با Generic Type Parameter:
abstract class Base {
    // public abstract void Method<T>() where T : Animal;  // ❌ Abstract methods نمی‌تونن generic constraints داشته باشن
}

// 2. Abstract method با Covariant Return Type:
abstract class Base {
    public abstract Animal GetAnimal();
}
class Derived : Base {
    // public override Dog GetAnimal() { }  // ❌ قبل از C# 9
    // ✅ C# 9+:
    public override Dog GetAnimal() => new Dog();  // ✅ Covariant Return Types
}
```

<div dir="rtl" style="text-align: right;">

### 🔬 Abstract Generic با Multiple Constraints:

<div dir="ltr" style="text-align: left;">

```csharp
interface IFlyable {
    void Fly();
}

abstract class AnimalProcessor<T> where T : Animal, IFlyable, new() {
    public abstract void Process(T animal);
    
    public T CreateAndProcess() {
        T animal = new T();  // ✅ new() constraint
        animal.Fly();       // ✅ IFlyable constraint
        Process(animal);     // ✅ Abstract method
        return animal;
    }
}

class BirdProcessor : AnimalProcessor<Bird> {
    public override void Process(Bird bird) {
        bird.Fly();
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته پیشرفته:

Abstract Generic Classes می‌تونن **Type Constraints** داشته باشن و این constraints به کلاس‌های مشتق **ارث می‌رسن**.

### ⚠️ محدودیت:

Abstract Methods نمی‌تونن **خودشون** Generic Type Parameters داشته باشن با Constraints! فقط کلاس می‌تونه Generic باشه.

