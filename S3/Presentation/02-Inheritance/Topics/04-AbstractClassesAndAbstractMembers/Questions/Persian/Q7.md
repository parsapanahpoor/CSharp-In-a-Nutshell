<div dir="rtl" style="text-align: right;">

## ❓ Q7:

## Abstract Methods به صورت ضمنی virtual هستند؟

### ✅ جواب:

بله! Abstract Methods **به صورت ضمنی virtual** هستند. نمی‌تونیم `virtual abstract` بنویسیم.

<div dir="ltr" style="text-align: left;">

```csharp
abstract class Animal {
    // این دو یکسان هستند:
    public abstract void Method1();  // ✅ به صورت ضمنی virtual است
    // public virtual abstract void Method1();  // ❌ Error! نمی‌تونیم virtual بنویسیم
}

class Dog : Animal {
    public override void Method1() {  // ✅ Override
        // ...
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

Abstract Methods **همیشه virtual** هستند چون باید override بشن. نیازی به نوشتن `virtual` نیست.

