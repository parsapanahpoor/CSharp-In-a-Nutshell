<div dir="rtl" style="text-align: right;">

## ❓ Q19:

## Type Erasure و Runtime Type Information در Generics چطور با Casting کار می‌کنه؟

### ✅ جواب عمیق:

برخلاف Java که **Type Erasure** داره، C# **Runtime Type Information** رو نگه می‌داره. این روی Casting تاثیر می‌ذاره.

### 🔍 Type Information در C#:

<div dir="ltr" style="text-align: left;">

```csharp
class Container<T> {
    public T Value { get; set; }
}

Container<int> intContainer = new Container<int> { Value = 42 };
Container<string> strContainer = new Container<string> { Value = "Hello" };

// در C#، Type Information حفظ می‌شه:
Type type1 = intContainer.GetType();   // Container`1[System.Int32]
Type type2 = strContainer.GetType();   // Container`1[System.String]

Console.WriteLine(type1 == type2);  // False ✅ (برخلاف Java!)
```

<div dir="rtl" style="text-align: right;">

### 🔬 Casting با Generic Types:

<div dir="ltr" style="text-align: left;">

```csharp
// ❌ نمی‌تونیم این کار رو بکنیم:
Container<Animal> animalContainer = new Container<Dog>();  
// Error: Cannot convert Container<Dog> to Container<Animal>

// چرا؟ چون Generic Types Invariant هستند (به طور پیش‌فرض)
```

<div dir="rtl" style="text-align: right;">

### ✅ راه حل: Covariant Interfaces

<div dir="ltr" style="text-align: left;">

```csharp
interface IContainer<out T> {
    T GetValue();
}

class Container<T> : IContainer<T> {
    private T value;
    public T GetValue() => value;
}

// حالا Covariance کار می‌کنه:
IContainer<Animal> animalContainer = new Container<Dog>();  // ✅
// چون IContainer<out T> است
```

<div dir="rtl" style="text-align: right;">

### 🔬 Runtime Type Checking:

<div dir="ltr" style="text-align: left;">

```csharp
Container<int> intContainer = new Container<int>();
Container<string> strContainer = new Container<string>();

// Runtime می‌تونه نوع Generic رو تشخیص بده:
if (intContainer is Container<int>) {  // ✅ True
    // ...
}

// حتی می‌تونیم Generic Type رو استخراج کنیم:
Type genericType = intContainer.GetType().GetGenericArguments()[0];
Console.WriteLine(genericType);  // System.Int32 ✅
```

<div dir="rtl" style="text-align: right;">

### 🎯 تفاوت با Java:

| ویژگی | C# | Java |
|-------|----|----|
| **Type Erasure** | ❌ ندارد | ✅ دارد |
| **Runtime Type Info** | ✅ دارد | ❌ ندارد |
| **Casting با Generics** | ✅ Type-safe | ⚠️ Type-unsafe |

### 💡 نتیجه:

C# **Type Safety** رو در runtime حفظ می‌کنه، اما Java در compile-time فقط.

