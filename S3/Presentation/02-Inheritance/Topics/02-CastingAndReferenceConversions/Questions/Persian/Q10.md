<div dir="rtl" style="text-align: right;">

## ❓ Q10:

## Pattern Matching با `is` چطور کار می‌کنه؟

### ✅ جواب:

Pattern Matching در C# 7+ به ما اجازه می‌ده **بررسی و کست** رو در یک خط انجام بدیم.

<div dir="ltr" style="text-align: left;">

```csharp
class Animal { }
class Dog : Animal {
    public string Name { get; set; }
}
class Cat : Animal {
    public int Age { get; set; }
}

Animal animal = new Dog { Name = "Buddy" };

// Pattern Matching:
if (animal is Dog d) {
    Console.WriteLine(d.Name);  // ✅ d از قبل Dog است
}

// با switch expression (C# 8+):
string sound = animal switch {
    Dog dog => $"Dog {dog.Name} barks",
    Cat cat => $"Cat is {cat.Age} years old",
    _ => "Unknown animal"
};
```

<div dir="rtl" style="text-align: right;">

### 🎯 مزایا:

- ✅ کوتاه‌تر و خوانا‌تر
- ✅ ایمن‌تر (کست خودکار)
- ✅ Scope محدود (متغیر فقط در if block)

### ⚠️ نکته:

متغیر فقط در **scope** مربوطه در دسترس است!

