<div dir="rtl" style="text-align: right;">

## ❓ Q4:

## عملگر `as` چیه؟

### ✅ جواب:

عملگر `as` یک **کست ایمن** انجام می‌ده. اگر موفق نشه، `null` برمی‌گردونه (بدون exception).

<div dir="ltr" style="text-align: left;">

```csharp
class Animal { }
class Dog : Animal { }
class Cat : Animal { }

Animal animal = new Cat();

Dog? dog = animal as Dog;  // ✅ null برمی‌گردونه (نه exception)
if (dog != null) {
    dog.Bark();
} else {
    Console.WriteLine("Not a Dog!");
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

`as` فقط با **reference types** کار می‌کنه. با value types کار نمی‌کنه.

