<div dir="rtl" style="text-align: right;">

## ❓ Q12:

## چرا `as` با value types کار نمی‌کنه؟

### ✅ جواب:

چون `as` در صورت شکست `null` برمی‌گردونه، اما value types نمی‌تونن `null` باشن!

<div dir="ltr" style="text-align: left;">

```csharp
int number = 42;
// int? result = number as int?;  // ❌ Error: as نمی‌تونه با value types کار کنه

// راه حل: از is استفاده کن
if (number is int i) {
    Console.WriteLine(i);  // ✅
}

// یا از nullable cast:
int? nullable = number as int?;  // ❌ بازم کار نمی‌کنه!
```

<div dir="rtl" style="text-align: right;">

### ✅ راه حل برای Value Types:

<div dir="ltr" style="text-align: left;">

```csharp
object obj = 42;

// روش 1: با is
if (obj is int i) {
    Console.WriteLine(i);  // ✅
}

// روش 2: Direct cast (با try-catch)
try {
    int num = (int)obj;  // ✅
} catch (InvalidCastException) {
    // handle
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

`as` فقط برای **reference types** طراحی شده. برای value types از `is` یا direct cast استفاده کن.

