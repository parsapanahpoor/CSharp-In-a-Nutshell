<div dir="rtl" style="text-align: right;">

## ❓ Q8:

## InvalidCastException چیه؟

### ✅ جواب:

وقتی سعی می‌کنیم یک شی رو به نوعی که **نمی‌تونه** تبدیل بشه، کست کنیم، این exception پرتاب می‌شه.

<div dir="ltr" style="text-align: left;">

```csharp
class Animal { }
class Dog : Animal { }
class Cat : Animal { }

Animal animal = new Cat();

try {
    Dog dog = (Dog)animal;  // ❌ InvalidCastException!
    // Cat نمی‌تونه به Dog تبدیل بشه
} catch (InvalidCastException ex) {
    Console.WriteLine($"Cast failed: {ex.Message}");
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 راه حل‌های ایمن:

<div dir="ltr" style="text-align: left;">

```csharp
// روش 1: با as
Dog? dog = animal as Dog;
if (dog != null) {
    dog.Bark();
}

// روش 2: با is
if (animal is Dog d) {
    d.Bark();
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

**همیشه** قبل از Downcast بررسی کن که آیا تبدیل ممکنه یا نه!

