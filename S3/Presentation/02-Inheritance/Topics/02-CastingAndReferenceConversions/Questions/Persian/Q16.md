<div dir="rtl" style="text-align: right;">

## ❓ Q16:

## Runtime Type Checking و Type Information در Casting چطور کار می‌کنه؟

### ✅ جواب عمیق:

هر شی در .NET یک **Type Handle** داره که اطلاعات نوع رو نگه می‌داره. Casting از این اطلاعات استفاده می‌کنه.

### 🔍 ساختار Type Information:

<div dir="ltr" style="text-align: left;">

```csharp
class Animal { }
class Dog : Animal { }
class Cat : Animal { }

Animal animal = new Dog();

// GetType() نوع واقعی رو برمی‌گردونه:
Type actualType = animal.GetType();  // Dog ✅
Type declaredType = typeof(Animal);   // Animal

Console.WriteLine(actualType == typeof(Dog));    // True ✅
Console.WriteLine(actualType == typeof(Animal)); // False
```

<div dir="rtl" style="text-align: right;">

### 🔬 مکانیزم Runtime Type Check:

<div dir="ltr" style="text-align: left;">

```csharp
// وقتی این کد رو می‌نویسی:
Dog dog = (Dog)animal;

// Runtime این کارها رو انجام می‌ده:
// 1. Type Handle رو از Object Header می‌خونه
// 2. Inheritance Chain رو چک می‌کنه
// 3. اگر Dog در chain باشه → OK
// 4. اگر نباشه → InvalidCastException
```

<div dir="rtl" style="text-align: right;">

### 📊 Inheritance Chain Check:

<div dir="ltr" style="text-align: left;">

```
Animal (Base)
  └─ Dog (Derived)
      └─ Puppy (Derived from Dog)

// وقتی animal is Puppy رو چک می‌کنیم:
// Runtime این chain رو بررسی می‌کنه:
// 1. آیا actual type = Puppy? → No
// 2. آیا Puppy در inheritance chain هست? → Yes (اگر animal واقعاً Puppy باشه)
```

<div dir="rtl" style="text-align: right;">

### ⚙️ Performance:

- **GetType()**: ~5-10 CPU cycles (باید Type Handle رو بخونه)
- **is operator**: ~1-2 CPU cycles (بهینه‌تر)
- **as operator**: ~1-2 CPU cycles (مثل is)
- **Direct cast**: ~1-2 CPU cycles + exception handling cost

### 🎯 نکته پیشرفته:

Type Information در **Metadata** ذخیره می‌شه و در runtime از طریق **Reflection** قابل دسترسی است.

