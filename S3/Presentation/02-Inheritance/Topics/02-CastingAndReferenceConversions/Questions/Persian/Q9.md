<div dir="rtl" style="text-align: right;">

## ❓ Q9:

## چرا Casting شی رو تغییر نمی‌ده؟

### ✅ جواب:

Casting فقط **نوع رفرنس** رو تغییر می‌ده، نه خود شی رو! شی در memory همون شی می‌مونه.

<div dir="ltr" style="text-align: left;">

```csharp
class Animal {
    public string Name { get; set; }
}

class Dog : Animal {
    public void Bark() { }
}

Dog dog = new Dog { Name = "Buddy" };
Animal animal = dog;  // Upcast

Console.WriteLine(animal.Name);  // "Buddy" ✅
Console.WriteLine(dog.Name);     // "Buddy" ✅

// هر دو به همون شی اشاره می‌کنن!
animal.Name = "Max";
Console.WriteLine(dog.Name);  // "Max" ✅ (همون شی!)
```

<div dir="rtl" style="text-align: right;">

### 🔍 در Memory:

<div dir="ltr" style="text-align: left;">

```
Heap:
┌─────────────┐
│ Dog Object  │ ← یک شی
│ Name: "Max"  │
└─────────────┘
     ↑    ↑
     |    |
dog  animal  (دو رفرنس به همون شی)
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

Casting **کپی نمی‌کنه**! فقط نوع رفرنس رو تغییر می‌ده.

