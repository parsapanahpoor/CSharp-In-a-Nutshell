<div dir="rtl" style="text-align: right;">

## ❓ Q17:

## Boxing/Unboxing و Casting چه تفاوتی دارن؟

### ✅ جواب عمیق:

**Boxing/Unboxing** تبدیل بین value type و object است، اما **Casting** تبدیل بین reference types است.

### 🔍 Boxing vs Casting:

<div dir="ltr" style="text-align: left;">

```csharp
// Boxing: value type → object
int number = 42;
object obj = number;  // ✅ Boxing (value type به reference type)

// Unboxing: object → value type
int num = (int)obj;   // ✅ Unboxing (reference type به value type)

// Casting: reference type → reference type
Animal animal = new Dog();
Dog dog = (Dog)animal;  // ✅ Casting (reference به reference)
```

<div dir="rtl" style="text-align: right;">

### 🔬 تفاوت‌های کلیدی:

| ویژگی | Boxing/Unboxing | Casting |
|-------|----------------|---------|
| **نوع تبدیل** | Value ↔ Reference | Reference ↔ Reference |
| **Memory** | کپی می‌کنه (Boxing) | فقط رفرنس تغییر می‌کنه |
| **Performance** | کندتر (heap allocation) | سریع (فقط type check) |
| **Exception** | InvalidCastException | InvalidCastException |

### ⚠️ مشکل Boxing:

<div dir="ltr" style="text-align: left;">

```csharp
int x = 42;
object obj = x;  // Boxing: یک object جدید در heap ساخته می‌شه
int y = (int)obj;  // Unboxing: از heap می‌خونه

// Performance overhead:
// 1. Heap allocation (Boxing)
// 2. Memory copy (Boxing)
// 3. Type check (Unboxing)
// 4. Memory read (Unboxing)
```

<div dir="rtl" style="text-align: right;">

### ✅ Casting (بدون Boxing):

<div dir="ltr" style="text-align: left;">

```csharp
Dog dog = new Dog();
Animal animal = dog;  // فقط رفرنس تغییر می‌کنه (هیچ کپی نمی‌شه)
Dog dog2 = (Dog)animal;  // فقط type check (خیلی سریع)
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته پیشرفته:

**Boxing** یک **object wrapper** در heap می‌سازه که value type رو نگه می‌داره. این **costly** است!
**Casting** فقط **type information** رو چک می‌کنه و هیچ memory allocation نداره.

### ⚠️ مهم:

از Boxing تا جایی که ممکنه **اجتناب کن**! Performance overhead داره.

