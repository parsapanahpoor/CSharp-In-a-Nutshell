<div dir="rtl" style="text-align: right;">

## ❓ Q5:

## عملگر `is` چیه؟

### ✅ جواب:

عملگر `is` برای **بررسی نوع** استفاده می‌شه. می‌تونه با **pattern matching** ترکیب بشه.

<div dir="ltr" style="text-align: left;">

```csharp
class Animal { }
class Dog : Animal { }
class Cat : Animal { }

Animal animal = new Dog();

// روش قدیمی:
if (animal is Dog) {
    Dog dog = (Dog)animal;
    dog.Bark();
}

// روش جدید (Pattern Matching - C# 7+):
if (animal is Dog d) {
    d.Bark();  // ✅ d از قبل Dog است!
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

`is` با pattern matching **کست و بررسی** رو در یک خط انجام می‌ده. خیلی راحت‌تره!

