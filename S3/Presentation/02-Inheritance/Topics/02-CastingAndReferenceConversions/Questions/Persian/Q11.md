<div dir="rtl" style="text-align: right;">

## ❓ Q11:

## می‌تونیم بین دو کلاس غیرمرتبط کست کنیم؟

### ❌ جواب: خیر!

فقط می‌تونیم بین کلاس‌هایی که در **سلسله‌مراتب Inheritance** هستند کست کنیم.

<div dir="ltr" style="text-align: left;">

```csharp
class Animal { }
class Dog : Animal { }
class Car { }  // هیچ ربطی به Animal نداره

Animal animal = new Dog();
Car car = (Car)animal;  // ❌ Error: Cannot convert Animal to Car
```

<div dir="rtl" style="text-align: right;">

### ✅ فقط این‌ها مجازند:

<div dir="ltr" style="text-align: left;">

```csharp
// 1. Upcast (Derived → Base):
Dog dog = new Dog();
Animal animal = dog;  // ✅

// 2. Downcast (Base → Derived):
Animal animal2 = new Dog();
Dog dog2 = (Dog)animal2;  // ✅ (اگر واقعاً Dog باشه)

// 3. Interface Implementation:
IEnumerable<int> list = new List<int>();  // ✅
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

Casting فقط در **سلسله‌مراتب Inheritance** یا **Interface Implementation** کار می‌کنه.

