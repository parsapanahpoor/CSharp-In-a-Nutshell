<div dir="rtl" style="text-align: right;">

## ❓ Q15:

## چرا بعضی وقت‌ها نیاز به کست داریم؟

### ✅ جواب:

وقتی کامپایلر نمی‌تونه **خودکار** نوع رو تشخیص بده، باید ما **صریح** کست کنیم.

<div dir="ltr" style="text-align: left;">

```csharp
class Animal { }
class Dog : Animal { }

// مثال 1: وقتی نوع متغیر مشخص نیست
object obj = new Dog();
Dog dog = (Dog)obj;  // ✅ باید کست کنیم

// مثال 2: وقتی از interface استفاده می‌کنیم
IEnumerable<int> numbers = new List<int> { 1, 2, 3 };
List<int> list = (List<int>)numbers;  // ✅ برای دسترسی به List methods

// مثال 3: وقتی از base reference استفاده می‌کنیم
Animal animal = new Dog();
Dog dog2 = (Dog)animal;  // ✅ برای دسترسی به Dog methods
```

<div dir="rtl" style="text-align: right;">

### 🎯 قانون کلی:

- **Upcast**: خودکار (نیاز به کست نداره)
- **Downcast**: صریح (باید کست کنیم)
- **Interface → Concrete**: صریح (باید کست کنیم)

### ⚠️ نکته:

اگر زیاد نیاز به کست داری، شاید **Design** کدت مشکل داره. بهتره از Polymorphism استفاده کنی!

