<div dir="ltr" style="text-align: left;">

## ❓ Q9:

## Why doesn't Casting change the object?

### ✅ Answer:

Casting only changes the **reference type**, not the object itself! The object in memory remains the same object.

<div dir="ltr" style="text-align: left;">

```csharp
class Animal {
    public string Name { get; set; }
}

class Dog : Animal {
    public void Bark() { }
}

Dog dog = new Dog { Name = "Buddy" };
Animal animal = dog;  // Upcast

Console.WriteLine(animal.Name);  // "Buddy" ✅
Console.WriteLine(dog.Name);     // "Buddy" ✅

// Both point to the same object!
animal.Name = "Max";
Console.WriteLine(dog.Name);  // "Max" ✅ (same object!)
```

<div dir="ltr" style="text-align: left;">

### 🔍 In Memory:

<div dir="ltr" style="text-align: left;">

```
Heap:
┌─────────────┐
│ Dog Object  │ ← One object
│ Name: "Max"  │
└─────────────┘
     ↑    ↑
     |    |
dog  animal  (two references to same object)
```

<div dir="ltr" style="text-align: left;">

### 🎯 Note:

Casting **doesn't copy**! It only changes the reference type.
