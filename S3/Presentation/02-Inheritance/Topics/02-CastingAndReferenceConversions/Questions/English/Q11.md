<div dir="ltr" style="text-align: left;">

## ❓ Q11:

## Can we cast between two unrelated classes?

### ❌ Answer: No!

We can only cast between classes that are in the **Inheritance hierarchy**.

<div dir="ltr" style="text-align: left;">

```csharp
class Animal { }
class Dog : Animal { }
class Car { }  // Has no relation to Animal

Animal animal = new Dog();
Car car = (Car)animal;  // ❌ Error: Cannot convert Animal to Car
```

<div dir="ltr" style="text-align: left;">

### ✅ Only these are allowed:

<div dir="ltr" style="text-align: left;">

```csharp
// 1. Upcast (Derived → Base):
Dog dog = new Dog();
Animal animal = dog;  // ✅

// 2. Downcast (Base → Derived):
Animal animal2 = new Dog();
Dog dog2 = (Dog)animal2;  // ✅ (if it's actually Dog)

// 3. Interface Implementation:
IEnumerable<int> list = new List<int>();  // ✅
```

<div dir="ltr" style="text-align: left;">

### 🎯 Note:

Casting only works in **Inheritance hierarchy** or **Interface Implementation**.
