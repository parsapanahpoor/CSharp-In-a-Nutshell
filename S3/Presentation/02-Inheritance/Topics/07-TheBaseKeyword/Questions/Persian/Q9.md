<div dir="rtl" style="text-align: right;">

## ❓ Q9:

## base در Interface Implementation چطور کار می‌کنه؟

### ✅ جواب:

`base` در Interface Implementation **کار نمی‌کنه** چون Interface implementation نداره!

<div dir="ltr" style="text-align: left;">

```csharp
interface IAnimal {
    void Speak();
}

class Animal : IAnimal {
    public void Speak() {
        Console.WriteLine("Animal");
    }
}

class Dog : Animal, IAnimal {
    public void Speak() {
        // base.Speak();  // ✅ Animal.Speak() (از Base Class)
        // اما base به Interface اشاره نمی‌کنه!
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

`base` فقط به **Base Class** اشاره می‌کنه، نه Interface.

