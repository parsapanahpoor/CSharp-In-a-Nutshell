<div dir="rtl" style="text-align: right;">

## ❓ Q11:

## base با Indexers چطور کار می‌کنه؟

### ✅ جواب:

`base` با Indexers هم کار می‌کنه. می‌تونیم Base Indexer رو صدا بزنیم.

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    protected int[] items = new int[10];
    
    public virtual int this[int index] {
        get => items[index];
        set => items[index] = value;
    }
}

class Derived : Base {
    public override int this[int index] {
        get => base[index] * 2;  // ✅ Base indexer
        set => base[index] = value / 2;
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

`base` با Indexers هم مثل Methods و Properties کار می‌کنه.

