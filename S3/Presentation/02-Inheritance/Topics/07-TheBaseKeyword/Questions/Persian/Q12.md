<div dir="rtl" style="text-align: right;">

## ❓ Q12:

## base در Multi-Level Inheritance چطور کار می‌کنه؟

### ✅ جواب:

`base` فقط به **مستقیم Base Class** اشاره می‌کنه. نمی‌تونه به GrandParent برسه.

<div dir="ltr" style="text-align: left;">

```csharp
class GrandParent {
    public virtual void Method() {
        Console.WriteLine("GrandParent");
    }
}

class Parent : GrandParent {
    public override void Method() {
        Console.WriteLine("Parent");
    }
}

class Child : Parent {
    public override void Method() {
        base.Method();  // ✅ Parent.Method() (مستقیم Base)
        // برای GrandParent باید از Cast استفاده کنیم:
        ((GrandParent)this).Method();  // ✅ GrandParent.Method()
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

`base` فقط **یک سطح بالا** می‌ره. برای GrandParent باید از Cast استفاده کنیم.

