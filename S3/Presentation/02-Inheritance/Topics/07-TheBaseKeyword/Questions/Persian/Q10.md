<div dir="rtl" style="text-align: right;">

## ❓ Q10:

## base در Constructor Chaining چطور کار می‌کنه؟

### ✅ جواب:

`base` در Constructor Chaining برای **صدا زدن Base Constructor** استفاده می‌شه.

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public Base() {
        Console.WriteLine("Base()");
    }
    
    public Base(string name) {
        Console.WriteLine($"Base({name})");
    }
}

class Derived : Base {
    public Derived() : base() {  // ✅ Base()
        Console.WriteLine("Derived()");
    }
    
    public Derived(string name) : base(name) {  // ✅ Base(name)
        Console.WriteLine($"Derived({name})");
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

`base()` باید **اول** در constructor صدا زده بشه. می‌تونیم پارامترها رو pass کنیم.

