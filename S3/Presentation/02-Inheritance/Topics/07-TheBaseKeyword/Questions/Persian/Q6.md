<div dir="rtl" style="text-align: right;">

## ❓ Q6:

## base با Fields چطور کار می‌کنه؟

### ✅ جواب:

`base` با Fields **مستقیم** کار نمی‌کنه. باید از Properties استفاده کنیم.

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    protected int value = 10;
}

class Derived : Base {
    public void Method() {
        // base.value;  // ❌ نمی‌تونیم مستقیماً استفاده کنیم
        value = 20;  // ✅ می‌تونیم مستقیماً استفاده کنیم (inherited)
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

Fields **inherited** می‌شن، پس نیازی به `base` ندارن. فقط برای Methods و Properties لازمه.

