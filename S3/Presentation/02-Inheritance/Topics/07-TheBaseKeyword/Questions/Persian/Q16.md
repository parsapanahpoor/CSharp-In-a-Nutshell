<div dir="rtl" style="text-align: right;">

## ❓ Q16:

## base Resolution و Method Lookup در Runtime چطور کار می‌کنه؟

### ✅ جواب عمیق:

`base` keyword در compile-time resolve می‌شه و به **مستقیم Base Class** اشاره می‌کنه.

### 🔍 Resolution Process:

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public virtual void Method() {
        Console.WriteLine("Base");
    }
}

class Derived : Base {
    public override void Method() {
        base.Method();  // چه اتفاقی می‌افته؟
    }
}
```

<div dir="rtl" style="text-align: right;">

### ⚙️ Compile-time Resolution:

**مرحله 1: Type Resolution**
- کامپایلر می‌بینه که `base` در `Derived` استفاده شده
- به Base Class می‌ره: `Base`

**مرحله 2: Member Lookup**
- در Base Class دنبال `Method()` می‌گرده
- پیدا می‌کنه: `Base.Method()`

**مرحله 3: Code Generation**
- کد تولید می‌شه که `Base.Method()` رو صدا می‌زنه
- **Direct Call** به Base Method (نه Virtual Call!)

<div dir="ltr" style="text-align: left;">

```csharp
// کامپایلر این کد:
base.Method();

// رو به این تبدیل می‌کنه:
Base.Method(this);  // Direct call (نه virtual!)
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته پیشرفته:

`base` **Direct Call** می‌زنه، نه Virtual Call! این یعنی همیشه Base Method صدا می‌شه، حتی اگر override شده باشه.

### ⚡ Performance:

- **base.Method()**: Direct Call (~1 CPU cycle) ✅
- **this.Method()**: Virtual Call (~2-3 CPU cycles) ⚠️

