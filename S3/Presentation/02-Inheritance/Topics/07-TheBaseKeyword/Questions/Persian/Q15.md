<div dir="rtl" style="text-align: right;">

## ❓ Q15:

## base در Constructor چه ترتیبی داره؟

### ✅ جواب:

`base` در Constructor **قبل از body** صدا زده می‌شه. Base Constructor اول اجرا می‌شه.

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public Base() {
        Console.WriteLine("Base constructor");
    }
}

class Derived : Base {
    public Derived() : base() {  // ✅ اول base صدا می‌شه
        Console.WriteLine("Derived constructor");
    }
}

new Derived();
// خروجی:
// Base constructor
// Derived constructor
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

ترتیب: **Base Constructor** → **Derived Constructor**

