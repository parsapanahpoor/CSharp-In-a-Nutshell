<div dir="rtl" style="text-align: right;">

## ❓ Q20:

## base با Design Patterns و Best Practices چطور کار می‌کنه؟

### ✅ جواب عمیق:

`base` در Design Patterns و Best Practices نقش مهمی داره.

### 🔍 Template Method Pattern:

<div dir="ltr" style="text-align: left;">

```csharp
abstract class DataProcessor {
    // Template Method:
    public void Process() {
        LoadData();
        ValidateData();
        TransformData();
        SaveData();
    }
    
    protected abstract void LoadData();
    protected virtual void ValidateData() {
        Console.WriteLine("Default validation");
    }
    protected abstract void TransformData();
    protected virtual void SaveData() {
        Console.WriteLine("Default save");
    }
}

class CsvProcessor : DataProcessor {
    protected override void LoadData() {
        Console.WriteLine("Loading CSV");
    }
    
    protected override void ValidateData() {
        base.ValidateData();  // ✅ Default validation
        Console.WriteLine("CSV-specific validation");
    }
    
    protected override void TransformData() {
        Console.WriteLine("Transforming CSV");
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🔬 Best Practices:

**1. همیشه base رو صدا بزن (اگر لازمه):**

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public virtual void Method() {
        // Important initialization
    }
}

class Derived : Base {
    public override void Method() {
        base.Method();  // ✅ Base initialization رو نگه می‌داریم
        // Extended behavior
    }
}
```

<div dir="rtl" style="text-align: right;">

**2. از base در Constructor استفاده کن:**

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    protected string name;
    public Base(string name) {
        this.name = name;
    }
}

class Derived : Base {
    public Derived(string name) : base(name) {  // ✅ Base initialization
        // Derived initialization
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته پیشرفته:

`base` برای **Template Method Pattern** و **Initialization** عالی است. همیشه base رو صدا بزن وقتی می‌خوای Base Behavior رو نگه داری.

### ⚠️ Anti-Pattern:

<div dir="ltr" style="text-align: left;">

```csharp
// ❌ بد: base رو صدا نمی‌زنه
class Derived : Base {
    public override void Method() {
        // Base behavior رو از دست می‌ده!
    }
}
```

<div dir="rtl" style="text-align: right;">

### 💡 توصیه:

**همیشه base رو صدا بزن** وقتی می‌خوای Base Behavior رو نگه داری و Extended Behavior اضافه کنی!

