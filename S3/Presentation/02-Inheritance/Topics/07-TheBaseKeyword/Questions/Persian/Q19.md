<div dir="rtl" style="text-align: right;">

## ❓ Q19:

## base در Constructor Initialization Order چطور کار می‌کنه؟

### ✅ جواب عمیق:

`base` در Constructor **قبل از Field Initialization** صدا می‌شه. ترتیب خیلی مهمه!

### 🔍 Initialization Order:

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    protected int baseField = Initialize("Base Field");
    
    public Base() {
        Console.WriteLine("Base Constructor");
    }
    
    private static int Initialize(string name) {
        Console.WriteLine($"Initializing: {name}");
        return 0;
    }
}

class Derived : Base {
    private int derivedField = Initialize("Derived Field");
    
    public Derived() : base() {  // ✅ base اول صدا می‌شه
        Console.WriteLine("Derived Constructor");
    }
    
    private static int Initialize(string name) {
        Console.WriteLine($"Initializing: {name}");
        return 0;
    }
}

new Derived();
```

<div dir="rtl" style="text-align: right;">

### 📊 ترتیب دقیق:

1. **Base Static Fields** (اگر وجود داشته باشن)
2. **Base Static Constructor**
3. **Derived Static Fields**
4. **Derived Static Constructor**
5. **Base Instance Fields**
6. **Base Constructor** (از `base()`)
7. **Derived Instance Fields**
8. **Derived Constructor Body**

<div dir="rtl" style="text-align: right;">

### 🎯 نکته پیشرفته:

`base()` **قبل از Derived Field Initialization** صدا می‌شه! این می‌تونه باعث bug بشه اگر در Base Constructor از Derived Fields استفاده کنیم.

### ⚠️ مشکل:

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public Base() {
        Initialize();  // ⚠️ Virtual method در constructor
    }
    
    protected virtual void Initialize() { }
}

class Derived : Base {
    private int value = 10;  // هنوز initialize نشده!
    
    protected override void Initialize() {
        Console.WriteLine(value);  // 0! ❌ (هنوز initialize نشده!)
    }
}
```

<div dir="rtl" style="text-align: right;">

### 💡 راه حل:

از Virtual Methods در Constructor استفاده **نکن**! یا از Initialize Pattern استفاده کن.

