<div dir="ltr" style="text-align: left;">

## ❓ Q17:

## How does base work with Generic Types and Type Parameters?

### ✅ Deep Answer:

`base` works in Generic Classes and preserves Type Parameters.

### 🔍 Generic base:

<div dir="ltr" style="text-align: left;">

```csharp
class Base<T> {
    public virtual void Process(T item) {
        Console.WriteLine($"Base processing {typeof(T).Name}");
    }
}

class Derived<T> : Base<T> {
    public override void Process(T item) {
        base.Process(item);  // ✅ Base<T>.Process(T)
        Console.WriteLine($"Derived processing {typeof(T).Name}");
    }
}
```

<div dir="ltr" style="text-align: left;">

### 🔬 Type Parameter Preservation:

<div dir="ltr" style="text-align: left;">

```csharp
class Base<T> where T : Animal {
    public virtual void Process(T animal) {
        animal.Eat();  // Knows T : Animal
    }
}

class Derived<T> : Base<T> where T : Animal {
    public override void Process(T animal) {
        base.Process(animal);  // ✅ Type parameter is preserved
        // Knows T : Animal
    }
}
```

<div dir="ltr" style="text-align: left;">

### 🎯 Advanced Note:

`base` in Generic Classes **preserves Type Parameters** and maintains Type Safety.

### ⚠️ Limitation:

`base` cannot change Type Parameters. Must have the same Type Parameters.
