<div dir="ltr" style="text-align: left;">

## ❓ Q5:

## How does base work with Properties?

### ✅ Answer:

`base` also works with Properties. We can call Base Property.

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public virtual int Value {
        get => 10;
        set { }
    }
}

class Derived : Base {
    public override int Value {
        get => base.Value * 2;  // ✅ Base property
        set => base.Value = value / 2;
    }
}
```

<div dir="ltr" style="text-align: left;">

### 🎯 Note:

`base` with Properties works like Methods.
