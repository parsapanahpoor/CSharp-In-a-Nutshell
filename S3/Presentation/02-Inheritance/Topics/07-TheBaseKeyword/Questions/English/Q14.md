<div dir="ltr" style="text-align: left;">

## ❓ Q14:

## Why is base necessary?

### ✅ Answer:

To **call Base Implementation** when we want to Override but also keep Base behavior.

<div dir="ltr" style="text-align: left;">

```csharp
class Printer {
    public virtual void Print(string text) {
        Console.WriteLine($"Printing: {text}");
    }
}

class AdvancedPrinter : Printer {
    public override void Print(string text) {
        Console.WriteLine("Pre-processing...");
        base.Print(text);  // ✅ Keep Base behavior
        Console.WriteLine("Post-processing...");
    }
}
```

<div dir="ltr" style="text-align: left;">

### 🎯 Note:

`base` allows us to keep **Base Behavior** and add **Extended Behavior**.
