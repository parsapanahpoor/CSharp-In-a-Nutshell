<div dir="ltr" style="text-align: left;">

## ❓ Q6:

## How does base work with Fields?

### ✅ Answer:

`base` doesn't work **directly** with Fields. We must use Properties.

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    protected int value = 10;
}

class Derived : Base {
    public void Method() {
        // base.value;  // ❌ Cannot use directly
        value = 20;  // ✅ Can use directly (inherited)
    }
}
```

<div dir="ltr" style="text-align: left;">

### 🎯 Note:

Fields are **inherited**, so no need for `base`. Only needed for Methods and Properties.
