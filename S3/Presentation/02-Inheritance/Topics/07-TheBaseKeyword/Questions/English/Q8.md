<div dir="ltr" style="text-align: left;">

## ❓ Q8:

## How does base work in Abstract Methods?

### ✅ Answer:

`base` doesn't work in Abstract Methods because Abstract Methods don't have implementation!

<div dir="ltr" style="text-align: left;">

```csharp
abstract class Base {
    public abstract void Method();  // ❌ Doesn't have implementation
}

class Derived : Base {
    public override void Method() {
        // base.Method();  // ❌ Error! Abstract method doesn't have implementation
        Console.WriteLine("Derived");
    }
}
```

<div dir="ltr" style="text-align: left;">

### 🎯 Note:

Abstract Methods **don't have implementation**, so we cannot use `base`.
