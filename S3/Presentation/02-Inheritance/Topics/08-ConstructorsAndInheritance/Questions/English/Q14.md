<div dir="ltr" style="text-align: left;">

## ❓ Q14:

## How do Constructors work with Optional Parameters?

### ✅ Answer:

Optional Parameters can replace Constructor Overloading.

<div dir="ltr" style="text-align: left;">

```csharp
class Person {
    public Person(string name, int age = 0) {  // ✅ Optional parameter
        Name = name;
        Age = age;
    }
}

// Usage:
var p1 = new Person("Ali");  // ✅ age = 0
var p2 = new Person("Ali", 25);  // ✅ age = 25
```

<div dir="ltr" style="text-align: left;">

### 🎯 Note:

Optional Parameters are **simpler** than Constructor Overloading.
