<div dir="ltr" style="text-align: left;">

## ❓ Q9:

## How does Abstract Class Constructor work?

### ✅ Answer:

Abstract Class can have Constructor, but cannot be directly instantiated.

<div dir="ltr" style="text-align: left;">

```csharp
abstract class Animal {
    protected string name;
    
    protected Animal(string name) {  // ✅ Abstract constructor
        this.name = name;
    }
}

class Dog : Animal {
    public Dog(string name) : base(name) {  // ✅ Must call base()
        // ...
    }
}
```

<div dir="ltr" style="text-align: left;">

### 🎯 Note:

Abstract Constructor is usually **protected** so it's only accessible from Derived Classes.
