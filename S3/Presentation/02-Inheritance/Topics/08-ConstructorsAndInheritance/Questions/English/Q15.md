<div dir="ltr" style="text-align: left;">

## ❓ Q15:

## How do Constructors work with Readonly Fields?

### ✅ Answer:

Readonly Fields only get values in **Constructor** or **Field Initializer**.

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    protected readonly string name;
    
    public Base(string name) {
        this.name = name;  // ✅ Only in constructor
    }
}

class Derived : Base {
    public Derived(string name) : base(name) {  // ✅ Call base()
        // name = "New";  // ❌ Error! Readonly field
    }
}
```

<div dir="ltr" style="text-align: left;">

### 🎯 Note:

Readonly Fields only get values in **Constructor** or **Field Initializer**.
