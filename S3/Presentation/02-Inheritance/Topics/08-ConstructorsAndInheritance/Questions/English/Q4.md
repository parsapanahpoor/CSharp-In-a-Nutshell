<div dir="ltr" style="text-align: left;">

## ❓ Q4:

## Why must we call base()?

### ✅ Answer:

If Base Class doesn't have **parameterless constructor**, we must call `base()`.

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public Base(string name) {  // ❌ Doesn't have parameterless constructor
        // ...
    }
}

class Derived : Base {
    public Derived(string name) : base(name) {  // ✅ Must call base()
        // ...
    }
}
```

<div dir="ltr" style="text-align: left;">

### 🎯 Note:

If Base Class only has parameterized constructor, we **must** call `base()`.
