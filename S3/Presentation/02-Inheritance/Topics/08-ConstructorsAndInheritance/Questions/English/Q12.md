<div dir="ltr" style="text-align: left;">

## ❓ Q12:

## Why are Virtual Methods dangerous in Constructors?

### ⚠️ Problem:

Calling Virtual Methods in Constructor is **dangerous** because Derived Fields are not yet initialized!

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public Base() {
        Initialize();  // ⚠️ Virtual method in constructor
    }
    
    protected virtual void Initialize() {
        Console.WriteLine("Base");
    }
}

class Derived : Base {
    private int value = 10;  // Not yet initialized!
    
    protected override void Initialize() {
        Console.WriteLine($"Derived: {value}");  // 0! ❌
    }
}
```

<div dir="ltr" style="text-align: left;">

### ✅ Solution:

**Don't** use Virtual Methods in Constructor! Or use Initialize Pattern.

<div dir="ltr" style="text-align: left;">

### 🎯 Note:

**Never** call Virtual Method in Constructor!
