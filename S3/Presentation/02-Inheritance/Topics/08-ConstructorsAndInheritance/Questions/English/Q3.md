<div dir="ltr" style="text-align: left;">

## ❓ Q3:

## What is base() in Constructor?

### ✅ Answer:

`base()` is used to **call Base Constructor**.

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public Base(string name) {
        Console.WriteLine($"Base: {name}");
    }
}

class Derived : Base {
    public Derived(string name) : base(name) {  // ✅ Base constructor
        Console.WriteLine("Derived");
    }
}
```

<div dir="ltr" style="text-align: left;">

### 🎯 Note:

`base()` must be called **first** in constructor. Before body.
