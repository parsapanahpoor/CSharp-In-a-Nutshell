<div dir="rtl" style="text-align: right;">

## ❓ Q4:

## چرا باید base() صدا بزنیم؟

### ✅ جواب:

اگر Base Class **بدون parameter constructor** نداشته باشه، باید `base()` صدا بزنیم.

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public Base(string name) {  // ❌ بدون parameter constructor نداره
        // ...
    }
}

class Derived : Base {
    public Derived(string name) : base(name) {  // ✅ باید base() صدا بزنیم
        // ...
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

اگر Base Class فقط parameterized constructor داشته باشه، **باید** `base()` صدا بزنیم.

