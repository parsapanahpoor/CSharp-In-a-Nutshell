<div dir="rtl" style="text-align: right;">

## ❓ Q8:

## می‌تونیم Constructor رو private کنیم؟

### ✅ جواب:

بله! Private Constructor برای **Singleton Pattern** یا **Static Classes** استفاده می‌شه.

<div dir="ltr" style="text-align: left;">

```csharp
class Singleton {
    private Singleton() { }  // ✅ Private constructor
    
    private static Singleton instance;
    public static Singleton Instance {
        get {
            if (instance == null) {
                instance = new Singleton();
            }
            return instance;
        }
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

Private Constructor **ارث‌بری** رو محدود می‌کنه. Derived Classes نمی‌تونن base() صدا بزنن.

