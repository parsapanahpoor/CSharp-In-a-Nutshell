<div dir="rtl" style="text-align: right;">

## ❓ Q15:

## Constructor با Readonly Fields چطور کار می‌کنه؟

### ✅ جواب:

Readonly Fields فقط در **Constructor** یا **Field Initializer** مقدار می‌گیرن.

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    protected readonly string name;
    
    public Base(string name) {
        this.name = name;  // ✅ فقط در constructor
    }
}

class Derived : Base {
    public Derived(string name) : base(name) {  // ✅ base() صدا می‌زنیم
        // name = "New";  // ❌ Error! Readonly field
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

Readonly Fields فقط در **Constructor** یا **Field Initializer** مقدار می‌گیرن.

