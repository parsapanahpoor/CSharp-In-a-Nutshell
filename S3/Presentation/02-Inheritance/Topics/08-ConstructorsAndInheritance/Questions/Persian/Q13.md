<div dir="rtl" style="text-align: right;">

## ❓ Q13:

## Constructor با this() چطور کار می‌کنه؟

### ✅ جواب:

`this()` برای **صدا زدن Constructor های همان کلاس** استفاده می‌شه.

<div dir="ltr" style="text-align: left;">

```csharp
class Person {
    public Person() : this("Unknown") { }  // ✅ this() chaining
    public Person(string name) {
        Name = name;
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

`this()` برای **Constructor Chaining** در همان کلاس استفاده می‌شه.

