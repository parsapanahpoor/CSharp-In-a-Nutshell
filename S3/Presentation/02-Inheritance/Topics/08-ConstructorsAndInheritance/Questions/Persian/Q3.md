<div dir="rtl" style="text-align: right;">

## ❓ Q3:

## base() در Constructor چیه؟

### ✅ جواب:

`base()` برای **صدا زدن Base Constructor** استفاده می‌شه.

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public Base(string name) {
        Console.WriteLine($"Base: {name}");
    }
}

class Derived : Base {
    public Derived(string name) : base(name) {  // ✅ Base constructor
        Console.WriteLine("Derived");
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

`base()` باید **اول** در constructor صدا زده بشه. قبل از body.

