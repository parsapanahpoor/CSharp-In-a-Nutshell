<div dir="rtl" style="text-align: right;">

## ❓ Q19:

## Constructor با Design Patterns و Best Practices چطور کار می‌کنن؟

### ✅ جواب عمیق:

Constructors در Design Patterns و Best Practices نقش مهمی دارن.

### 🔍 Factory Pattern:

<div dir="ltr" style="text-align: left;">

```csharp
abstract class Animal {
    protected Animal(string name) {
        Name = name;
    }
    
    public string Name { get; }
    
    public static Animal Create(string type, string name) {
        return type switch {
            "Dog" => new Dog(name),
            "Cat" => new Cat(name),
            _ => throw new ArgumentException()
        };
    }
}

class Dog : Animal {
    public Dog(string name) : base(name) { }
}

class Cat : Animal {
    public Cat(string name) : base(name) { }
}
```

<div dir="rtl" style="text-align: right;">

### 🔬 Builder Pattern:

<div dir="ltr" style="text-align: left;">

```csharp
class Person {
    private Person(string name, int age) {  // ✅ Private constructor
        Name = name;
        Age = age;
    }
    
    public string Name { get; }
    public int Age { get; }
    
    public class Builder {
        private string name;
        private int age;
        
        public Builder WithName(string name) {
            this.name = name;
            return this;
        }
        
        public Builder WithAge(int age) {
            this.age = age;
            return this;
        }
        
        public Person Build() {
            return new Person(name, age);
        }
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 Best Practices:

1. **همیشه base() صدا بزن** (اگر لازمه)
2. **از Virtual Methods در Constructor استفاده نکن**
3. **Constructor رو ساده نگه دار**
4. **از Factory Pattern استفاده کن** برای Complex Construction

### ⚠️ Anti-Pattern:

<div dir="ltr" style="text-align: left;">

```csharp
// ❌ بد: Virtual method در constructor
class Base {
    public Base() {
        Initialize();  // ⚠️ خطرناک!
    }
    
    protected virtual void Initialize() { }
}
```

<div dir="rtl" style="text-align: right;">

### 💡 توصیه:

**Constructor رو ساده نگه دار**! Complex Logic رو در Initialize() methods بذار.

