<div dir="rtl" style="text-align: right;">

## ❓ Q17:

## Constructor با Generic Types و Type Constraints چطور کار می‌کنه؟

### ✅ جواب عمیق:

Generic Constructors می‌تونن خیلی پیچیده بشن، مخصوصاً وقتی با Type Constraints ترکیب می‌شن.

### 🔍 Generic Constructor:

<div dir="ltr" style="text-align: left;">

```csharp
class Base<T> where T : Animal {
    protected T item;
    
    public Base(T item) {
        this.item = item;
        item.Eat();  // می‌دونه T : Animal است
    }
}

class Derived<T> : Base<T> where T : Animal {
    public Derived(T item) : base(item) {  // ✅ base() با Generic
        // می‌دونه T : Animal است
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🔬 Type Constraints:

<div dir="ltr" style="text-align: left;">

```csharp
class Container<T> where T : Animal, new() {
    private T item;
    
    public Container() : base() {
        item = new T();  // ✅ new() constraint
        item.Eat();      // ✅ Animal constraint
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته پیشرفته:

Generic Constructors **Type Constraints** رو preserve می‌کنن و Type Safety رو حفظ می‌کنن.

### ⚠️ محدودیت:

Constructor نمی‌تونه **خودش** Generic Type Parameters داشته باشه! فقط کلاس می‌تونه Generic باشه.

