<div dir="rtl" style="text-align: right;">

## ❓ Q10:

## Constructor با Field Initialization چطور کار می‌کنه؟

### ✅ جواب:

Field Initialization **قبل از Constructor Body** انجام می‌شه.

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    private int value = 10;  // ✅ Field initialization
    
    public Base() {
        Console.WriteLine($"Base: {value}");  // 10 ✅
    }
}

class Derived : Base {
    private int value = 20;  // ✅ Field initialization
    
    public Derived() {
        Console.WriteLine($"Derived: {value}");  // 20 ✅
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

ترتیب: **Base Fields** → **Base Constructor** → **Derived Fields** → **Derived Constructor**

