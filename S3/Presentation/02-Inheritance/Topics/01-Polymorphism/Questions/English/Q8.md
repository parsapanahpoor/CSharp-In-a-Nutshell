<div dir="ltr" style="text-align: left;">

## ❓ Q8:

## Why does the derived method execute when using a base reference?

### ✅ Answer:

Because at **runtime**, the actual type of the object (Real Type) is checked, not the reference type!

<div dir="ltr" style="text-align: left;">

```csharp
class Shape {
    public virtual void Draw() => Console.WriteLine("Drawing shape");
}

class Circle : Shape {
    public override void Draw() => Console.WriteLine("Drawing circle ⭕");
}

class Rectangle : Shape {
    public override void Draw() => Console.WriteLine("Drawing rectangle ▭");
}

// Reference: Shape | Actual type: Circle
Shape shape1 = new Circle();
shape1.Draw();  // Output: "Drawing circle ⭕" ✅

// Reference: Shape | Actual type: Rectangle
Shape shape2 = new Rectangle();
shape2.Draw();  // Output: "Drawing rectangle ▭" ✅
```

<div dir="ltr" style="text-align: left;">

### 🎯 Note:

**Virtual Method Table (VMT)** checks the actual type at runtime and calls the correct method.
