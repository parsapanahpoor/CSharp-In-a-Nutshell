<div dir="ltr" style="text-align: left;">

## ❓ Q6:

## Why is Polymorphism useful?

### 💡 Problem without Polymorphism:

<div dir="ltr" style="text-align: left;">

```csharp
// Must write separate method for each animal type:
void ProcessDog(Dog dog) {
    dog.Speak();
}

void ProcessCat(Cat cat) {
    cat.Speak();
}

void ProcessBird(Bird bird) {
    bird.Speak();
}
```

<div dir="ltr" style="text-align: left;">

### ✅ Solution with Polymorphism:

<div dir="ltr" style="text-align: left;">

```csharp
// Just one method for all:
void ProcessAnimal(Animal animal) {
    animal.Speak();  // It knows what to say!
}

ProcessAnimal(new Dog());   // ✅
ProcessAnimal(new Cat());   // ✅
ProcessAnimal(new Bird());  // ✅
```

<div dir="ltr" style="text-align: left;">

### 🎯 Advantages:

- ✅ Less and cleaner code
- ✅ Easier maintenance
- ✅ More extensible
- ✅ Reduced coupling
