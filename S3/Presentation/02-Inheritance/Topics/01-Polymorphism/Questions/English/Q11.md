<div dir="ltr" style="text-align: left;">

## ❓ Q11:

## Can we override a non-virtual method?

### ❌ Answer: No!

<div dir="ltr" style="text-align: left;">

```csharp
class Animal {
    public void Speak() {  // ❌ Not virtual
        Console.WriteLine("Animal sound");
    }
}

class Dog : Animal {
    public override void Speak() {  // ❌ Error!
        Console.WriteLine("Woof!");
    }
}
```

<div dir="ltr" style="text-align: left;">

### Error:
```
Error: 'Dog.Speak()': cannot override inherited member 'Animal.Speak()' 
because it is not marked virtual, abstract, or override
```

### ✅ Solution:

<div dir="ltr" style="text-align: left;">

```csharp
class Animal {
    public virtual void Speak() {  // ✅ Added virtual
        Console.WriteLine("Animal sound");
    }
}

class Dog : Animal {
    public override void Speak() {  // ✅ Now it works
        Console.WriteLine("Woof!");
    }
}
```

<div dir="ltr" style="text-align: left;">

### 🎯 Note:

Only methods marked `virtual`, `abstract`, or `override` can be overridden.
