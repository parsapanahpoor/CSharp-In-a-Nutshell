<div dir="ltr" style="text-align: left;">

## ❓ Q7:

## Why are virtual and override necessary?

### ❌ Without virtual/override:

<div dir="ltr" style="text-align: left;">

```csharp
class Animal {
    public void Speak() {  // ❌ Not virtual
        Console.WriteLine("Animal sound");
    }
}

class Dog : Animal {
    public void Speak() {  // ❌ Not override (Hiding!)
        Console.WriteLine("Woof!");
    }
}

Animal animal = new Dog();
animal.Speak();  // Output: "Animal sound" ❌ (not "Woof!")
```

<div dir="ltr" style="text-align: left;">

### ✅ With virtual/override:

<div dir="ltr" style="text-align: left;">

```csharp
class Animal {
    public virtual void Speak() {  // ✅ virtual
        Console.WriteLine("Animal sound");
    }
}

class Dog : Animal {
    public override void Speak() {  // ✅ override
        Console.WriteLine("Woof!");
    }
}

Animal animal = new Dog();
animal.Speak();  // Output: "Woof!" ✅
```

<div dir="ltr" style="text-align: left;">

### 🎯 Note:

Without `virtual`/`override`, the derived method is **hidden** (Hiding), not Overridden!
