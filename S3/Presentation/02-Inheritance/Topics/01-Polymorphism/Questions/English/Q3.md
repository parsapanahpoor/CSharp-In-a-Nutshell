<div dir="ltr" style="text-align: left;">

## ❓ Q3:

## What is Method Overloading?

### ✅ Answer:

Method Overloading means having multiple methods with the **same name** but **different parameters**.

<div dir="ltr" style="text-align: left;">

```csharp
class Calculator {
    // All are Add, but parameters differ:
    public int Add(int a, int b) => a + b;
    
    public double Add(double a, double b) => a + b;
    
    public int Add(int a, int b, int c) => a + b + c;
    
    public string Add(string a, string b) => a + b;
}

var calc = new Calculator();
calc.Add(2, 3);           // ✅ First method
calc.Add(2.5, 3.7);       // ✅ Second method
calc.Add(1, 2, 3);        // ✅ Third method
calc.Add("Hello", "World"); // ✅ Fourth method
```

<div dir="ltr" style="text-align: left;">

### 🎯 Note:

The compiler decides which method to call based on the **number and type** of parameters.
