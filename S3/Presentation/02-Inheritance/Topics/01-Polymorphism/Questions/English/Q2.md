<div dir="ltr" style="text-align: left;">

## ❓ Q2:

## How many types of Polymorphism do we have?

### ✅ Answer:

Two main types:

### 1️⃣ Static Polymorphism
- Decision is made at **compile time**
- Example: Method Overloading

### 2️⃣ Dynamic Polymorphism
- Decision is made at **runtime**
- Example: Method Overriding

<div dir="ltr" style="text-align: left;">

```csharp
// Static (Compile-time)
class Calculator {
    public int Add(int a, int b) => a + b;
    public double Add(double a, double b) => a + b;  // Overload
}

// Dynamic (Runtime)
class Animal {
    public virtual void Speak() { }
}
class Dog : Animal {
    public override void Speak() => Console.WriteLine("Woof!");
}
```

<div dir="ltr" style="text-align: left;">

### 🎯 Note:

Static = Compiler decides | Dynamic = Runtime decides
