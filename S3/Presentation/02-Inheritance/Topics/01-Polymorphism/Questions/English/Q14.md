<div dir="ltr" style="text-align: left;">

## ❓ Q14:

## How much is the Performance Overhead in Polymorphism?

### ✅ Answer:

Polymorphism has a **Virtual Method Call** that is slightly slower than Direct Call.

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public void DirectMethod() { }      // ✅ Direct call (fast)
    public virtual void VirtualMethod() { } // ⚠️ Virtual call (slightly slower)
}

class Derived : Base {
    public override void VirtualMethod() { }
}

Base obj = new Derived();

// Direct Call (without Polymorphism):
obj.DirectMethod();  // ✅ Goes directly to Base.DirectMethod (very fast)

// Virtual Call (with Polymorphism):
obj.VirtualMethod();  // ⚠️ Must check VMT (slightly slower)
```

<div dir="ltr" style="text-align: left;">

### 📊 Performance Comparison:

- **Direct Call**: ~1 CPU cycle
- **Virtual Call**: ~2-3 CPU cycles (must check VMT)

### 🎯 Note:

This overhead is **very negligible** and doesn't matter in 99% of cases. The benefits of Polymorphism (flexibility, maintainability) far outweigh this small cost!

### ⚠️ Only in Hot Paths (like very repetitive loops) it might matter.
