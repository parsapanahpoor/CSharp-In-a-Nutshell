<div dir="ltr" style="text-align: left;">

## ❓ Q1:

## What is Polymorphism?

### ✅ Answer:

**Polymorphism** means "many forms" - the ability of an object to take different forms.

### Simple Example:

<div dir="ltr" style="text-align: left;">

```csharp
// An Animal reference can hold different objects:
Animal animal1 = new Dog();    // ✅
Animal animal2 = new Cat();    // ✅
Animal animal3 = new Bird();   // ✅

// All of these are Animals, but they have different behaviors!
```

<div dir="ltr" style="text-align: left;">

### 🎯 Note:

Polymorphism allows us to work with different types (Dog, Cat, Bird) through a single interface (like `Animal`).
