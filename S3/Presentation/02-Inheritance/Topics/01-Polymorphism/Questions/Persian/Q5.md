<div dir="rtl" style="text-align: right;">

## ❓ Q5:

## تفاوت Overloading و Overriding چیه؟

### ✅ مقایسه:

| ویژگی | Overloading | Overriding |
|-------|-------------|------------|
| **زمان تصمیم** | Compile-time | Runtime |
| **کلاس** | یک کلاس | کلاس پایه + مشتق |
| **نام متد** | یکسان | یکسان |
| **پارامترها** | باید متفاوت باشند | باید یکسان باشند |
| **کلیدواژه** | نیاز نداره | `virtual` + `override` |

<div dir="ltr" style="text-align: left;">

```csharp
// Overloading (یک کلاس)
class Math {
    public int Add(int a, int b) => a + b;
    public double Add(double a, double b) => a + b;  // ✅
}

// Overriding (دو کلاس)
class Animal {
    public virtual void Speak() { }
}
class Dog : Animal {
    public override void Speak() { }  // ✅
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

Overloading = چند متد با نام یکسان | Overriding = بازنویسی متد پایه

