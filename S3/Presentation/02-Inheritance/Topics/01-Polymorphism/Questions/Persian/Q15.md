<div dir="rtl" style="text-align: right;">

## ❓ Q15:

## Covariant Return Types در Polymorphism چیه؟

### ✅ جواب:

در C# 9+، می‌تونیم Return Type رو در Override تغییر بدیم (باید Derived Type باشه).

### ❌ قبل از C# 9:

<div dir="ltr" style="text-align: left;">

```csharp
class Animal { }
class Dog : Animal { }

class AnimalFactory {
    public virtual Animal Create() => new Animal();
}

class DogFactory : AnimalFactory {
    public override Animal Create() => new Dog();  
    // ❌ باید Animal برگردونیم، نه Dog
}

var factory = new DogFactory();
Animal animal = factory.Create();  // ✅
Dog dog = factory.Create();  // ❌ Error: Cannot convert Animal to Dog
```

<div dir="rtl" style="text-align: right;">

### ✅ C# 9+ (Covariant Return Types):

<div dir="ltr" style="text-align: left;">

```csharp
class AnimalFactory {
    public virtual Animal Create() => new Animal();
}

class DogFactory : AnimalFactory {
    public override Dog Create() => new Dog();  
    // ✅ می‌تونیم Dog برگردونیم (Covariant)
}

var factory = new DogFactory();
Animal animal = factory.Create();  // ✅ Upcast
Dog dog = factory.Create();  // ✅ مستقیم Dog می‌گیریم!
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

**Covariance** یعنی می‌تونیم نوع Derived رو جای نوع Base برگردونیم. این فقط برای Return Types کار می‌کنه، نه Parameter Types!

### ⚠️ محدودیت:

فقط در C# 9.0 و بالاتر پشتیبانی می‌شه. در نسخه‌های قدیمی‌تر باید همان نوع Base رو برگردونیم.

