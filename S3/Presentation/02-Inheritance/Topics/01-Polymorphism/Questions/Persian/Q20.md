<div dir="rtl" style="text-align: right;">

## ❓ Q20:

## Polymorphism با Generic Constraints و Type Erasure چطور کار می‌کنه؟

### ✅ جواب عمیق:

این یکی از پیچیده‌ترین موارد Polymorphism است که Generic Types و Inheritance رو ترکیب می‌کنه.

### 🔍 مشکل Type Erasure در Generics:

<div dir="ltr" style="text-align: left;">

```csharp
class Animal {
    public virtual void Speak() => Console.WriteLine("Animal");
}

class Dog : Animal {
    public override void Speak() => Console.WriteLine("Woof");
}

// Generic Method:
void Process<T>(T item) where T : Animal {
    item.Speak();  // این چطور کار می‌کنه؟
}

Process(new Dog());  // Polymorphism کار می‌کنه؟
```

<div dir="rtl" style="text-align: right;">

### ✅ Runtime Behavior:

<div dir="ltr" style="text-align: left;">

```csharp
// در زمان کامپایل، C# این رو به این تبدیل می‌کنه:
void Process<T>(T item) where T : Animal {
    // کامپایلر می‌دونه T : Animal است
    // پس می‌تونه Animal.Speak() رو صدا بزنه
    // اما چون virtual است، Runtime نوع واقعی رو چک می‌کنه
    ((Animal)item).Speak();  // Virtual Call → Runtime Resolution
}

Process<Dog>(new Dog());  // ✅ Dog.Speak() اجرا می‌شه
```

<div dir="rtl" style="text-align: right;">

### 🔬 مثال پیچیده‌تر:

<div dir="ltr" style="text-align: left;">

```csharp
interface IProcessor<in T> {
    void Process(T item);
}

class AnimalProcessor : IProcessor<Animal> {
    public void Process(Animal animal) {
        animal.Speak();  // Virtual Call
    }
}

class DogProcessor : AnimalProcessor, IProcessor<Dog> {
    public void Process(Dog dog) {
        dog.Bark();  // Dog-specific method
    }
}

// Contravariance در Action:
IProcessor<Animal> processor = new DogProcessor();
processor.Process(new Dog());  // ✅
processor.Process(new Cat());  // ✅

// اما:
DogProcessor dp = new DogProcessor();
dp.Process(new Dog());  // DogProcessor.Process(Dog) ✅
((IProcessor<Animal>)dp).Process(new Dog());  // AnimalProcessor.Process(Animal) ✅
```

<div dir="rtl" style="text-align: right;">

### 🎯 Virtual Method Resolution در Generic Context:

<div dir="ltr" style="text-align: left;">

```csharp
class Base<T> where T : Animal {
    public virtual void Handle(T item) {
        item.Speak();  // Virtual Call
    }
}

class Derived<T> : Base<T> where T : Animal {
    public override void Handle(T item) {
        base.Handle(item);  // Base.Handle() → Virtual Call
        item.Move();  // اگر T متد Move داشته باشه
    }
}

Derived<Dog> d = new Derived<Dog>();
d.Handle(new Dog());  
// 1. Derived<Dog>.Handle(Dog) صدا زده می‌شه
// 2. base.Handle() → Base<Dog>.Handle(Dog) → Virtual Call
// 3. item.Speak() → Dog.Speak() (Runtime Resolution)
```

<div dir="rtl" style="text-align: right;">

### ⚠️ محدودیت‌های پیچیده:

<div dir="ltr" style="text-align: left;">

```csharp
// ❌ نمی‌تونیم این کار رو بکنیم:
class Base {
    public virtual void Process<T>(T item) where T : Animal {
        item.Speak();
    }
}

class Derived : Base {
    // ❌ نمی‌تونیم constraint رو تغییر بدیم:
    public override void Process<T>(T item) where T : Dog {  
        // Error: Constraints must match base method
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکات کلیدی:

1. **Generic Constraints** در compile-time چک می‌شن
2. **Virtual Method Resolution** در runtime انجام می‌شه
3. **Type Erasure** در C# وجود نداره (برخلاف Java)
4. **Covariance/Contravariance** فقط در Interfaces و Delegates
5. **Generic Virtual Methods** می‌تونن خیلی پیچیده بشن

### 💡 نتیجه:

Polymorphism با Generics ترکیب قدرتمندی است، اما باید دقت کنیم که **Type Safety** حفظ بشه و **Runtime Resolution** درست کار کنه.

