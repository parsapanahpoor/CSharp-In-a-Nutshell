<div dir="rtl" style="text-align: right;">

## ❓ Q3:

## Method Overloading چیه؟

### ✅ جواب:

Method Overloading یعنی چند متد با **نام یکسان** اما **پارامترهای متفاوت** داشته باشیم.

<div dir="ltr" style="text-align: left;">

```csharp
class Calculator {
    // همه Add هستند، اما پارامترها فرق می‌کنند:
    public int Add(int a, int b) => a + b;
    
    public double Add(double a, double b) => a + b;
    
    public int Add(int a, int b, int c) => a + b + c;
    
    public string Add(string a, string b) => a + b;
}

var calc = new Calculator();
calc.Add(2, 3);           // ✅ متد اول
calc.Add(2.5, 3.7);       // ✅ متد دوم
calc.Add(1, 2, 3);        // ✅ متد سوم
calc.Add("Hello", "World"); // ✅ متد چهارم
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

کامپایلر بر اساس **تعداد و نوع** پارامترها تصمیم می‌گیره کدوم متد رو صدا بزنه.

