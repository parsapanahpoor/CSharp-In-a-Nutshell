<div dir="rtl" style="text-align: right;">

## ❓ Q13:

## چرا Polymorphism برای Design Patterns مهمه؟

### ✅ جواب:

بیشتر Design Patterns بر پایه Polymorphism هستند!

### مثال: Strategy Pattern

<div dir="ltr" style="text-align: left;">

```csharp
// Strategy Interface
interface IPaymentMethod {
    void Pay(double amount);
}

// Concrete Strategies
class CreditCard : IPaymentMethod {
    public void Pay(double amount) => 
        Console.WriteLine($"Paid ${amount} with Credit Card");
}

class PayPal : IPaymentMethod {
    public void Pay(double amount) => 
        Console.WriteLine($"Paid ${amount} with PayPal");
}

// Context
class ShoppingCart {
    private IPaymentMethod paymentMethod;
    
    public void SetPaymentMethod(IPaymentMethod method) {
        paymentMethod = method;  // ✅ Polymorphism
    }
    
    public void Checkout(double total) {
        paymentMethod.Pay(total);  // ✅ هر روش پرداخت خودش می‌دونه چطور پرداخت کنه
    }
}

// استفاده:
var cart = new ShoppingCart();
cart.SetPaymentMethod(new CreditCard());
cart.Checkout(100);  // ✅

cart.SetPaymentMethod(new PayPal());
cart.Checkout(200);  // ✅
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

Polymorphism به ما اجازه می‌ده **الگوریتم‌ها رو در زمان اجرا عوض کنیم** بدون تغییر کد اصلی.

