<div dir="rtl" style="text-align: right;">

## ❓ Q7:

## virtual و override چرا لازمه؟

### ❌ بدون virtual/override:

<div dir="ltr" style="text-align: left;">

```csharp
class Animal {
    public void Speak() {  // ❌ virtual نیست
        Console.WriteLine("Animal sound");
    }
}

class Dog : Animal {
    public void Speak() {  // ❌ override نیست (Hiding!)
        Console.WriteLine("Woof!");
    }
}

Animal animal = new Dog();
animal.Speak();  // خروجی: "Animal sound" ❌ (نه "Woof!")
```

<div dir="rtl" style="text-align: right;">

### ✅ با virtual/override:

<div dir="ltr" style="text-align: left;">

```csharp
class Animal {
    public virtual void Speak() {  // ✅ virtual
        Console.WriteLine("Animal sound");
    }
}

class Dog : Animal {
    public override void Speak() {  // ✅ override
        Console.WriteLine("Woof!");
    }
}

Animal animal = new Dog();
animal.Speak();  // خروجی: "Woof!" ✅
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

بدون `virtual`/`override`، متد مشتق شده **پنهان** می‌شه (Hiding)، نه Override!

