<div dir="rtl" style="text-align: right;">

## ❓ Q17:

## چرا Contravariance در Method Parameters پشتیبانی نمی‌شه؟

### ✅ جواب عمیق:

این یک **محدودیت Type Safety** است که C# برای جلوگیری از خطاهای Runtime اعمال می‌کنه.

### ❌ اگر Contravariance مجاز بود:

<div dir="ltr" style="text-align: left;">

```csharp
class Animal { }
class Dog : Animal { }
class Cat : Animal { }

class Base {
    public virtual void Process(Dog dog) {
        dog.Bark();  // فقط Dog می‌تونه Bark کنه
    }
}

class Derived : Base {
    // فرض کنیم Contravariance مجاز بود:
    public override void Process(Animal animal) {  // ❌ خطا می‌ده
        // حالا animal می‌تونه Dog باشه یا Cat!
        animal.Bark();  // ❌ Cat نمی‌تونه Bark کنه!
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🔍 مشکل Type Safety:

<div dir="ltr" style="text-align: left;">

```csharp
Base obj = new Derived();
Dog myDog = new Dog();

// Base.Process() انتظار Dog داره:
obj.Process(myDog);  

// اما Derived.Process() انتظار Animal داره!
// اگر Cat بفرستیم چی؟
Cat myCat = new Cat();
obj.Process(myCat);  // ❌ Derived.Process() اجرا می‌شه
                      // اما Derived.Process() فکر می‌کنه Animal داره
                      // و ممکنه متدهای Dog رو صدا بزنه → Runtime Error!
```

<div dir="rtl" style="text-align: right;">

### ✅ راه حل: Interface با Contravariance

<div dir="ltr" style="text-align: left;">

```csharp
// در Interfaces می‌تونیم Contravariance داشته باشیم:
interface IProcessor<in T> {  // 'in' = Contravariant
    void Process(T item);
}

IProcessor<Animal> processor = new DogProcessor();  // ✅
// حالا می‌تونیم Animal بفرستیم:
processor.Process(new Dog());  // ✅
processor.Process(new Cat());  // ✅
```

<div dir="rtl" style="text-align: right;">

### 🎯 تفاوت:

- **Virtual Methods**: Contravariance مجاز نیست (Type Safety)
- **Generic Interfaces**: Contravariance با `in` keyword مجاز است
- **Delegates**: Contravariance در parameter types مجاز است

### 💡 دلیل:

Virtual Methods باید **Liskov Substitution Principle** رو رعایت کنن. Contravariance این اصل رو نقض می‌کنه.

