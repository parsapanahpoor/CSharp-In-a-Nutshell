<div dir="rtl" style="text-align: right;">

## ❓ Q9:

## می‌تونیم متد virtual رو override نکنیم؟

### ✅ جواب:

بله! اجباری نیست. اگر override نکنیم، متد کلاس پایه اجرا می‌شه.

<div dir="ltr" style="text-align: left;">

```csharp
class Animal {
    public virtual void Speak() {
        Console.WriteLine("Animal makes sound");
    }
}

class Dog : Animal {
    // override نکردیم! ✅ مشکلی نیست
}

class Cat : Animal {
    public override void Speak() {
        Console.WriteLine("Meow!");
    }
}

Animal dog = new Dog();
dog.Speak();  // خروجی: "Animal makes sound" (از کلاس پایه)

Animal cat = new Cat();
cat.Speak();  // خروجی: "Meow!" (از کلاس مشتق)
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

Override **اختیاری** است. اگر نکنیم، رفتار کلاس پایه استفاده می‌شه.

