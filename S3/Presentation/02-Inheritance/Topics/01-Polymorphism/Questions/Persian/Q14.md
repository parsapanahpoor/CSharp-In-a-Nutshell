<div dir="rtl" style="text-align: right;">

## ❓ Q14:

## Performance Overhead در Polymorphism چقدره؟

### ✅ جواب:

Polymorphism یک **Virtual Method Call** داره که کمی کندتر از Direct Call است.

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public void DirectMethod() { }      // ✅ Direct call (سریع)
    public virtual void VirtualMethod() { } // ⚠️ Virtual call (کمی کندتر)
}

class Derived : Base {
    public override void VirtualMethod() { }
}

Base obj = new Derived();

// Direct Call (بدون Polymorphism):
obj.DirectMethod();  // ✅ مستقیم به Base.DirectMethod می‌ره (خیلی سریع)

// Virtual Call (با Polymorphism):
obj.VirtualMethod();  // ⚠️ باید VMT رو چک کنه (کمی کندتر)
```

<div dir="rtl" style="text-align: right;">

### 📊 مقایسه Performance:

- **Direct Call**: ~1 CPU cycle
- **Virtual Call**: ~2-3 CPU cycles (باید VMT رو چک کنه)

### 🎯 نکته:

این overhead **خیلی ناچیز** است و در 99% موارد مهم نیست. مزایای Polymorphism (انعطاف، نگهداری) خیلی بیشتر از این هزینه کوچیکه!

### ⚠️ فقط در Hot Path ها (مثل حلقه‌های خیلی تکراری) ممکنه مهم باشه.

