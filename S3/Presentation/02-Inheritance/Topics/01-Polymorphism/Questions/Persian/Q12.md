<div dir="rtl" style="text-align: right;">

## ❓ Q12:

## Overloading با پارامترهای optional چطور کار می‌کنه؟

### ✅ جواب:

Optional parameters می‌تونه باعث ابهام در Overloading بشه!

<div dir="ltr" style="text-align: left;">

```csharp
class Calculator {
    public int Add(int a, int b) => a + b;
    
    public int Add(int a, int b = 0, int c = 0) => a + b + c;
}

var calc = new Calculator();
calc.Add(2, 3);  // ❌ Error: Ambiguous call!
```

<div dir="rtl" style="text-align: right;">

### خطا:
```
Error: The call is ambiguous between 'Add(int, int)' and 'Add(int, int, int)'
```

### ✅ راه حل:

<div dir="ltr" style="text-align: left;">

```csharp
class Calculator {
    // فقط یک متد با optional parameters:
    public int Add(int a, int b = 0, int c = 0) => a + b + c;
}

calc.Add(2);      // ✅ a=2, b=0, c=0
calc.Add(2, 3);   // ✅ a=2, b=3, c=0
calc.Add(2, 3, 4); // ✅ a=2, b=3, c=4
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

از Optional Parameters به جای Overloading استفاده کن، یا نام‌ها رو متفاوت کن.

