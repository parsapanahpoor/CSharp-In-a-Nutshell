<div dir="rtl" style="text-align: right;">

## ❓ Q19:

## Sealed Override و Performance Optimization چیه؟

### ✅ جواب عمیق:

`sealed override` به کامپایلر می‌گه که این متد دیگه override نمی‌شه و می‌تونه **Optimization** انجام بده.

### 🔍 بدون Sealed:

<div dir="ltr" style="text-align: left;">

```csharp
class Animal {
    public virtual void Speak() => Console.WriteLine("Animal");
}

class Dog : Animal {
    public override void Speak() => Console.WriteLine("Woof");
    // ممکنه کلاس دیگه‌ای از Dog ارث ببره و Speak رو override کنه
}

class Puppy : Dog {
    public override void Speak() => Console.WriteLine("Yip");  // ✅ مجاز
}

Animal a = new Dog();
a.Speak();  // باید VMT رو چک کنه (Virtual Call)
```

<div dir="rtl" style="text-align: right;">

### ✅ با Sealed Override:

<div dir="ltr" style="text-align: left;">

```csharp
class Animal {
    public virtual void Speak() => Console.WriteLine("Animal");
}

class Dog : Animal {
    public sealed override void Speak() => Console.WriteLine("Woof");
    // ✅ دیگه نمی‌شه override کرد
}

class Puppy : Dog {
    public override void Speak() { }  // ❌ Error: Cannot override sealed method
}

Animal a = new Dog();
a.Speak();  // کامپایلر می‌تونه Direct Call بزنه! (Optimization)
```

<div dir="rtl" style="text-align: right;">

### ⚡ Optimization Opportunities:

<div dir="ltr" style="text-align: left;">

```csharp
// کامپایلر می‌تونه این کد:
Animal a = new Dog();
a.Speak();  // Virtual Call

// رو به این تبدیل کنه (اگر sealed باشه):
Dog d = new Dog();
d.Speak();  // Direct Call (سریع‌تر!)
```

<div dir="rtl" style="text-align: right;">

### 📊 Performance Impact:

| حالت | Call Type | CPU Cycles | بهینه‌سازی |
|------|-----------|------------|------------|
| Virtual (بدون sealed) | Virtual Call | 2-3 | ❌ |
| Sealed Override | Direct Call | 1 | ✅ |
| Non-virtual | Direct Call | 1 | ✅ |

### 🎯 مزایای Sealed:

1. **Performance**: کامپایلر می‌تونه Direct Call بزنه
2. **Security**: جلوگیری از تغییر رفتار ناخواسته
3. **Documentation**: نشان می‌ده که این متد final است
4. **JIT Optimization**: JIT Compiler می‌تونه بهتر optimize کنه

### ⚠️ نکته:

این optimization در **Release mode** و با **JIT optimization** فعال بیشتر موثره. در Debug mode ممکنه همیشه Virtual Call بزنه.

