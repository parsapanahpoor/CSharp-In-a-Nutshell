<div dir="rtl" style="text-align: right;">

## ❓ Q11:

## آیا می‌تونیم متد non-virtual رو override کنیم؟

### ❌ جواب: خیر!

<div dir="ltr" style="text-align: left;">

```csharp
class Animal {
    public void Speak() {  // ❌ virtual نیست
        Console.WriteLine("Animal sound");
    }
}

class Dog : Animal {
    public override void Speak() {  // ❌ Error!
        Console.WriteLine("Woof!");
    }
}
```

<div dir="rtl" style="text-align: right;">

### خطا:
```
Error: 'Dog.Speak()': cannot override inherited member 'Animal.Speak()' 
because it is not marked virtual, abstract, or override
```

### ✅ راه حل:

<div dir="ltr" style="text-align: left;">

```csharp
class Animal {
    public virtual void Speak() {  // ✅ virtual اضافه کردیم
        Console.WriteLine("Animal sound");
    }
}

class Dog : Animal {
    public override void Speak() {  // ✅ حالا کار می‌کنه
        Console.WriteLine("Woof!");
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

فقط متدهای `virtual`, `abstract` یا `override` رو می‌شه override کرد.

