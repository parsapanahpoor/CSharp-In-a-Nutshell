<div dir="rtl" style="text-align: right;">

## ❓ Q6:

## چرا Polymorphism مفیده؟

### 💡 مشکل بدون Polymorphism:

<div dir="ltr" style="text-align: left;">

```csharp
// باید برای هر نوع حیوان متد جداگانه بنویسیم:
void ProcessDog(Dog dog) {
    dog.Speak();
}

void ProcessCat(Cat cat) {
    cat.Speak();
}

void ProcessBird(Bird bird) {
    bird.Speak();
}
```

<div dir="rtl" style="text-align: right;">

### ✅ راه حل با Polymorphism:

<div dir="ltr" style="text-align: left;">

```csharp
// فقط یک متد برای همه:
void ProcessAnimal(Animal animal) {
    animal.Speak();  // خودش می‌دونه چی بگه!
}

ProcessAnimal(new Dog());   // ✅
ProcessAnimal(new Cat());   // ✅
ProcessAnimal(new Bird());  // ✅
```

<div dir="rtl" style="text-align: right;">

### 🎯 مزایا:

- ✅ کد کمتر و تمیزتر
- ✅ نگهداری آسان‌تر
- ✅ توسعه‌پذیری بیشتر
- ✅ کاهش وابستگی

