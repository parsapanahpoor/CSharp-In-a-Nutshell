<div dir="ltr" style="text-align: left;">

## ❓ Q16:

## How exactly does Overload Resolution Algorithm work?

### ✅ Deep Answer:

C# compiler has a **complex algorithm** for Overload Resolution that happens in several steps.

### 🔍 Resolution Algorithm:

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public void Method(int x) { }
    public void Method(object x) { }
}

class Derived : Base {
    public void Method(double x) { }
}

Derived d = new Derived();
d.Method(5);  // What happens?
```

<div dir="ltr" style="text-align: left;">

### 📊 Resolution Steps:

**Step 1: Candidate Methods**
- All accessible methods (from Derived and Base)
- `Method(int)`, `Method(object)`, `Method(double)`

**Step 2: Applicable Methods**
- Methods compatible with argument type
- `Method(5)` → `int`
- ✅ `Method(int)` - Exact match
- ✅ `Method(object)` - Implicit conversion
- ✅ `Method(double)` - Implicit conversion

**Step 3: Best Match (Type Conversion Cost)**
- Exact match: `Method(int)` → Cost = 0 ⭐
- Implicit conversion: `Method(object)` → Cost = 1
- Implicit conversion: `Method(double)` → Cost = 1

**Result**: `Method(int)` from Base is selected! ✅

<div dir="ltr" style="text-align: left;">

```csharp
d.Method(5);     // Base.Method(int) ✅
d.Method(5.0);   // Derived.Method(double) ✅
d.Method("Hi");  // Base.Method(object) ✅
```

<div dir="ltr" style="text-align: left;">

### 🎯 Advanced Note:

**Derived methods** have priority (if exact match), but **Base methods** also participate in Resolution.

### ⚙️ Type Conversion Cost:

| Conversion | Cost | Example |
|-----------|------|------|
| **Exact Match** | 0 | `int` → `int` |
| **Implicit Numeric** | 1 | `int` → `double` |
| **Implicit Reference** | 1 | `Dog` → `Animal` |
| **Boxing** | 1 | `int` → `object` |
| **Explicit Conversion** | 2+ | `(int)double` |
