<div dir="ltr" style="text-align: left;">

## ❓ Q17:

## How does Overload Resolution work with Generic Type Inference?

### ✅ Deep Answer:

Generic Type Inference can make Overload Resolution more complex.

### 🔍 Type Inference:

<div dir="ltr" style="text-align: left;">

```csharp
class Test {
    public void Method<T>(T item) {
        Console.WriteLine($"Generic: {typeof(T).Name}");
    }
    
    public void Method(int x) {
        Console.WriteLine("Int");
    }
    
    public void Method(object x) {
        Console.WriteLine("Object");
    }
}

var test = new Test();
test.Method(5);  // What happens?
```

<div dir="ltr" style="text-align: left;">

### 📊 Resolution Process:

**Step 1: Candidate Methods**
- `Method<T>(T)` (Generic)
- `Method(int)` (Non-generic)
- `Method(object)` (Non-generic)

**Step 2: Type Inference**
- For `Method<T>(T)`: T = int (inferred)
- Becomes `Method<int>(int)`

**Step 3: Best Match**
- `Method(int)` → Exact match (Cost = 0) ⭐
- `Method<int>(int)` → Exact match (Cost = 0)
- `Method(object)` → Implicit conversion (Cost = 1)

**Result**: `Method(int)` is selected! ✅ (Non-generic has priority)

<div dir="ltr" style="text-align: left;">

```csharp
test.Method(5);    // Method(int) ✅
test.Method("Hi");  // Method<string>(string) ✅ (Generic)
test.Method(5.0);  // Method<double>(double) ✅ (Generic)
```

<div dir="ltr" style="text-align: left;">

### 🎯 Advanced Note:

**Non-generic Methods** have priority. Generic Methods are only used when Non-generic doesn't match.

### ⚠️ Ambiguity:

<div dir="ltr" style="text-align: left;">

```csharp
class Test {
    public void Method<T>(T item) where T : Animal { }
    public void Method<T>(T item) where T : Dog { }
}

// test.Method(new Dog());  // ❌ Ambiguous! (Dog : Animal)
```

<div dir="ltr" style="text-align: left;">

### 💡 Solution:

Use Explicit Type Parameter:

<div dir="ltr" style="text-align: left;">

```csharp
test.Method<Dog>(new Dog());  // ✅ Explicit type
```

<div dir="ltr" style="text-align: left;">
