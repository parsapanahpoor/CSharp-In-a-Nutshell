<div dir="ltr" style="text-align: left;">

## ❓ Q11:

## How does Overloading work with Interface Implementation?

### ✅ Answer:

Interface Methods also participate in Overload Resolution.

<div dir="ltr" style="text-align: left;">

```csharp
interface IProcessor {
    void Process(int x);
}

class Processor : IProcessor {
    public void Process(int x) { }  // Interface implementation
    public void Process(string s) { }  // ✅ Overload
}

var proc = new Processor();
proc.Process(5);    // Process(int) ✅
proc.Process("Hi"); // Process(string) ✅
```

<div dir="ltr" style="text-align: left;">

### 🎯 Note:

Interface Methods also participate in Resolution. Compiler checks all accessible methods.
