<div dir="ltr" style="text-align: left;">

## ❓ Q5:

## What is Ambiguous Overload Resolution?

### ⚠️ Problem:

When compiler cannot find **best match**, it gives Ambiguous Error.

<div dir="ltr" style="text-align: left;">

```csharp
class Test {
    public void Method(int x, double y) { }
    public void Method(double x, int y) { }
}

var test = new Test();
test.Method(5, 5);  // ❌ Error: Ambiguous call!
// Compiler doesn't know which to choose
```

<div dir="ltr" style="text-align: left;">

### ✅ Solution:

<div dir="ltr" style="text-align: left;">

```csharp
// Explicit cast:
test.Method(5, (double)5);  // ✅ Method(int, double)
test.Method((double)5, 5);  // ✅ Method(double, int)
```

<div dir="ltr" style="text-align: left;">

### 🎯 Note:

Ambiguous Resolution happens when **no method is better** than others.
