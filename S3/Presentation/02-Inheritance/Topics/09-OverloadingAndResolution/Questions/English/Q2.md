<div dir="ltr" style="text-align: left;">

## ❓ Q2:

## What is Method Resolution?

### ✅ Answer:

**Method Resolution** means compiler selects the **best method** from among Overloads.

<div dir="ltr" style="text-align: left;">

```csharp
class Math {
    public int Add(int a, int b) => a + b;
    public double Add(double a, double b) => a + b;
}

var math = new Math();
math.Add(5, 3);  // Compiler selects Add(int, int) ✅
```

<div dir="ltr" style="text-align: left;">

### 🎯 Note:

Resolution happens at **Compile-time**. Compiler finds the best match.
