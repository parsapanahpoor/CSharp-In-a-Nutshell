<div dir="ltr" style="text-align: left;">

## ❓ Q9:

## How does Overloading work with Null?

### ✅ Answer:

`null` matches with **Reference Types**, not Value Types.

<div dir="ltr" style="text-align: left;">

```csharp
class Test {
    public void Method(string s) { }  // ✅ null matches
    public void Method(int x) { }     // ❌ null doesn't match
}

var test = new Test();
test.Method(null);  // Method(string) is selected ✅
```

<div dir="ltr" style="text-align: left;">

### 🎯 Note:

`null` only matches with **Reference Types**. If multiple Reference Types exist, gives Ambiguous Error.
