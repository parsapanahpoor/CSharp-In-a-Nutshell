<div dir="rtl" style="text-align: right;">

## ❓ Q15:

## چه موقع از Overloading استفاده کنیم؟

### ✅ جواب:

وقتی می‌خوایم **همان عمل** رو با **انواع مختلف** انجام بدیم.

### مثال‌ها:

1. **Math Operations**: `Add(int)`, `Add(double)`, `Add(decimal)`
2. **String Operations**: `Format(string)`, `Format(string, object)`
3. **Collection Operations**: `Add(item)`, `AddRange(items)`

<div dir="ltr" style="text-align: left;">

```csharp
class Calculator {
    public int Add(int a, int b) => a + b;
    public double Add(double a, double b) => a + b;
    public decimal Add(decimal a, decimal b) => a + b;
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

Overloading برای **همان عمل با انواع مختلف** استفاده می‌شه. اگر عمل متفاوته، از نام‌های مختلف استفاده کن.

