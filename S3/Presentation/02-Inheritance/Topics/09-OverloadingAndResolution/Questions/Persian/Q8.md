<div dir="rtl" style="text-align: right;">

## ❓ Q8:

## Overloading با params چطور کار می‌کنه؟

### ✅ جواب:

`params` در Overload Resolution **کمترین اولویت** رو داره.

<div dir="ltr" style="text-align: left;">

```csharp
class Test {
    public void Method(int x) { }           // ✅ اولویت اول
    public void Method(params int[] x) { } // ⚠️ اولویت آخر
}

var test = new Test();
test.Method(5);  // Method(int) انتخاب می‌شه ✅ (نه params)
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

`params` فقط وقتی استفاده می‌شه که **هیچ match بهتری** وجود نداشته باشه.

