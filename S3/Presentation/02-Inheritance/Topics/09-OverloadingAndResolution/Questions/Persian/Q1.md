<div dir="rtl" style="text-align: right;">

## ❓ Q1:

## Method Overloading چیه؟

### ✅ جواب:

**Method Overloading** یعنی چند متد با **نام یکسان** اما **پارامترهای متفاوت** داشته باشیم.

<div dir="ltr" style="text-align: left;">

```csharp
class Calculator {
    public int Add(int a, int b) => a + b;
    public double Add(double a, double b) => a + b;  // ✅ Overload
    public int Add(int a, int b, int c) => a + b + c;  // ✅ Overload
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

Overloading بر اساس **تعداد، نوع یا ترتیب** پارامترها انجام می‌شه.

