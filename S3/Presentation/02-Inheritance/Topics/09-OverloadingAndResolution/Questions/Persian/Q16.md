<div dir="rtl" style="text-align: right;">

## ❓ Q16:

## Overload Resolution Algorithm دقیقا چطور کار می‌کنه؟

### ✅ جواب عمیق:

کامپایلر C# یک **الگوریتم پیچیده** برای Overload Resolution داره که در چند مرحله انجام می‌شه.

### 🔍 Resolution Algorithm:

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public void Method(int x) { }
    public void Method(object x) { }
}

class Derived : Base {
    public void Method(double x) { }
}

Derived d = new Derived();
d.Method(5);  // چه اتفاقی می‌افته؟
```

<div dir="rtl" style="text-align: right;">

### 📊 مراحل Resolution:

**مرحله 1: Candidate Methods**
- همه متدهای قابل دسترسی (از Derived و Base)
- `Method(int)`, `Method(object)`, `Method(double)`

**مرحله 2: Applicable Methods**
- متدهایی که با argument type سازگار هستند
- `Method(5)` → `int` است
- ✅ `Method(int)` - Exact match
- ✅ `Method(object)` - Implicit conversion
- ✅ `Method(double)` - Implicit conversion

**مرحله 3: Best Match (Type Conversion Cost)**
- Exact match: `Method(int)` → Cost = 0 ⭐
- Implicit conversion: `Method(object)` → Cost = 1
- Implicit conversion: `Method(double)` → Cost = 1

**نتیجه**: `Method(int)` از Base انتخاب می‌شه! ✅

<div dir="ltr" style="text-align: left;">

```csharp
d.Method(5);     // Base.Method(int) ✅
d.Method(5.0);   // Derived.Method(double) ✅
d.Method("Hi");  // Base.Method(object) ✅
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته پیشرفته:

**Derived methods** اولویت دارند (اگر exact match باشند)، اما **Base methods** هم در Resolution شرکت می‌کنن.

### ⚙️ Type Conversion Cost:

| Conversion | Cost | مثال |
|-----------|------|------|
| **Exact Match** | 0 | `int` → `int` |
| **Implicit Numeric** | 1 | `int` → `double` |
| **Implicit Reference** | 1 | `Dog` → `Animal` |
| **Boxing** | 1 | `int` → `object` |
| **Explicit Conversion** | 2+ | `(int)double` |

