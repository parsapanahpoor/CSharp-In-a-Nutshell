<div dir="rtl" style="text-align: right;">

## ❓ Q4:

## Overloading با Inheritance چطور کار می‌کنه؟

### ✅ جواب:

متدهای **Inherited** هم در Overload Resolution شرکت می‌کنن.

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public void Process(int x) {
        Console.WriteLine("Base: int");
    }
}

class Derived : Base {
    public void Process(string s) {
        Console.WriteLine("Derived: string");
    }
}

Derived d = new Derived();
d.Process(10);   // Base.Process(int) ✅ (inherited)
d.Process("Hi"); // Derived.Process(string) ✅
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

Inherited Methods هم در Resolution شرکت می‌کنن. کامپایلر همه متدهای قابل دسترسی رو بررسی می‌کنه.

