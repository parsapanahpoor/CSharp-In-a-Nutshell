<div dir="rtl" style="text-align: right;">

## ❓ Q18:

## Overload Resolution با Covariance و Contravariance چطور کار می‌کنه?

### ✅ جواب عمیق:

Covariance و Contravariance در Overload Resolution تاثیر دارن، اما فقط در **Interfaces** و **Delegates**.

### 🔍 Covariance در Interfaces:

<div dir="ltr" style="text-align: left;">

```csharp
class Animal { }
class Dog : Animal { }

interface IProcessor<out T> {
    void Process(T item);
}

class Processor<T> : IProcessor<T> {
    public void Process(T item) { }
}

// Covariance:
IProcessor<Animal> processor = new Processor<Dog>();  // ✅
processor.Process(new Dog());  // ✅
```

<div dir="rtl" style="text-align: right;">

### ⚠️ اما در Classes:

<div dir="ltr" style="text-align: left;">

```csharp
class Processor<T> {
    public void Process(T item) { }
}

// ❌ نمی‌تونیم این کار رو بکنیم:
// Processor<Animal> p = new Processor<Dog>();  // Error: Generic types invariant
```

<div dir="rtl" style="text-align: right;">

### 🔬 Overload Resolution:

<div dir="ltr" style="text-align: left;">

```csharp
class Test {
    public void Method(Animal a) { }
    public void Method(Dog d) { }
}

var test = new Test();
Animal animal = new Dog();
test.Method(animal);  // Method(Animal) ✅ (نوع رفرنس مهمه)
test.Method(new Dog());  // Method(Dog) ✅ (نوع واقعی مهمه)
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته پیشرفته:

Overload Resolution بر اساس **نوع compile-time** انجام می‌شه، نه runtime type. Covariance فقط در Interfaces کار می‌کنه.

### ⚠️ محدودیت:

Generic Classes **Invariant** هستند. Covariance/Contravariance فقط در Interfaces و Delegates.

