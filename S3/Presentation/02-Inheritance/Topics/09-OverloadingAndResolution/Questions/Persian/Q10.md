<div dir="rtl" style="text-align: right;">

## ❓ Q10:

## Overloading با Generic Methods چطور کار می‌کنه؟

### ✅ جواب:

Generic Methods هم می‌تونن overload بشن، اما Type Inference مهمه.

<div dir="ltr" style="text-align: left;">

```csharp
class Test {
    public void Method<T>(T item) {
        Console.WriteLine($"Generic: {typeof(T).Name}");
    }
    
    public void Method(int x) {
        Console.WriteLine("Int");
    }
}

var test = new Test();
test.Method(5);  // Method(int) انتخاب می‌شه ✅ (Non-generic اولویت داره)
test.Method("Hi");  // Method<string>(string) ✅
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

Non-generic Methods **اولویت** دارن. Generic Methods فقط وقتی استفاده می‌شن که Non-generic match نباشه.

