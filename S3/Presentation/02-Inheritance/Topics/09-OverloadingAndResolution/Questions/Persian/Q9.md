<div dir="rtl" style="text-align: right;">

## ❓ Q9:

## Overloading با Null چطور کار می‌کنه؟

### ✅ جواب:

`null` با **Reference Types** match می‌کنه، نه Value Types.

<div dir="ltr" style="text-align: left;">

```csharp
class Test {
    public void Method(string s) { }  // ✅ null match می‌کنه
    public void Method(int x) { }     // ❌ null match نمی‌کنه
}

var test = new Test();
test.Method(null);  // Method(string) انتخاب می‌شه ✅
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

`null` فقط با **Reference Types** match می‌کنه. اگر چند Reference Type باشه، Ambiguous Error می‌ده.

