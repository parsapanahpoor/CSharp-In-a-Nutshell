<div dir="rtl" style="text-align: right;">

## ❓ Q17:

## Overload Resolution با Generic Type Inference چطور کار می‌کنه؟

### ✅ جواب عمیق:

Generic Type Inference می‌تونه Overload Resolution رو پیچیده‌تر کنه.

### 🔍 Type Inference:

<div dir="ltr" style="text-align: left;">

```csharp
class Test {
    public void Method<T>(T item) {
        Console.WriteLine($"Generic: {typeof(T).Name}");
    }
    
    public void Method(int x) {
        Console.WriteLine("Int");
    }
    
    public void Method(object x) {
        Console.WriteLine("Object");
    }
}

var test = new Test();
test.Method(5);  // چه اتفاقی می‌افته؟
```

<div dir="rtl" style="text-align: right;">

### 📊 Resolution Process:

**مرحله 1: Candidate Methods**
- `Method<T>(T)` (Generic)
- `Method(int)` (Non-generic)
- `Method(object)` (Non-generic)

**مرحله 2: Type Inference**
- برای `Method<T>(T)`: T = int (inferred)
- `Method<int>(int)` می‌شه

**مرحله 3: Best Match**
- `Method(int)` → Exact match (Cost = 0) ⭐
- `Method<int>(int)` → Exact match (Cost = 0)
- `Method(object)` → Implicit conversion (Cost = 1)

**نتیجه**: `Method(int)` انتخاب می‌شه! ✅ (Non-generic اولویت داره)

<div dir="ltr" style="text-align: left;">

```csharp
test.Method(5);    // Method(int) ✅
test.Method("Hi");  // Method<string>(string) ✅ (Generic)
test.Method(5.0);  // Method<double>(double) ✅ (Generic)
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته پیشرفته:

**Non-generic Methods** اولویت دارن. Generic Methods فقط وقتی استفاده می‌شن که Non-generic match نباشه.

### ⚠️ Ambiguity:

<div dir="ltr" style="text-align: left;">

```csharp
class Test {
    public void Method<T>(T item) where T : Animal { }
    public void Method<T>(T item) where T : Dog { }
}

// test.Method(new Dog());  // ❌ Ambiguous! (Dog : Animal است)
```

<div dir="rtl" style="text-align: right;">

### 💡 راه حل:

از Explicit Type Parameter استفاده کن:
<div dir="ltr" style="text-align: left;">

```csharp
test.Method<Dog>(new Dog());  // ✅ Explicit type
```

<div dir="rtl" style="text-align: right;">

