<div dir="rtl" style="text-align: right;">

## ❓ Q5:

## Ambiguous Overload Resolution چیه؟

### ⚠️ مشکل:

وقتی کامپایلر نمی‌تونه **بهترین match** رو پیدا کنه، Ambiguous Error می‌ده.

<div dir="ltr" style="text-align: left;">

```csharp
class Test {
    public void Method(int x, double y) { }
    public void Method(double x, int y) { }
}

var test = new Test();
test.Method(5, 5);  // ❌ Error: Ambiguous call!
// کامپایلر نمی‌دونه کدوم رو انتخاب کنه
```

<div dir="rtl" style="text-align: right;">

### ✅ راه حل:

<div dir="ltr" style="text-align: left;">

```csharp
// Explicit cast:
test.Method(5, (double)5);  // ✅ Method(int, double)
test.Method((double)5, 5);  // ✅ Method(double, int)
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

Ambiguous Resolution وقتی پیش می‌آد که **هیچ متدی بهتر** از بقیه نیست.

