<div dir="rtl" style="text-align: right;">

## ❓ Q11:

## Overloading با Interface Implementation چطور کار می‌کنه؟

### ✅ جواب:

Interface Methods هم در Overload Resolution شرکت می‌کنن.

<div dir="ltr" style="text-align: left;">

```csharp
interface IProcessor {
    void Process(int x);
}

class Processor : IProcessor {
    public void Process(int x) { }  // Interface implementation
    public void Process(string s) { }  // ✅ Overload
}

var proc = new Processor();
proc.Process(5);    // Process(int) ✅
proc.Process("Hi"); // Process(string) ✅
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

Interface Methods هم در Resolution شرکت می‌کنن. کامپایلر همه متدهای قابل دسترسی رو بررسی می‌کنه.

