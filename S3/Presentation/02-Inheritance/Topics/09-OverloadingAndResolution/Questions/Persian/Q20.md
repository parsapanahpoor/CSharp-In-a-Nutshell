<div dir="rtl" style="text-align: right;">

## ❓ Q20:

## Overload Resolution با Design Patterns و Best Practices چطور کار می‌کنه?

### ✅ جواب عمیق:

Overload Resolution در Design Patterns و Best Practices نقش مهمی داره.

### 🔍 Factory Pattern با Overloading:

<div dir="ltr" style="text-align: left;">

```csharp
class AnimalFactory {
    public static Animal Create(string type) {
        return type switch {
            "Dog" => new Dog(),
            "Cat" => new Cat(),
            _ => throw new ArgumentException()
        };
    }
    
    // Overload برای convenience:
    public static Animal CreateDog() => new Dog();
    public static Animal CreateCat() => new Cat();
}
```

<div dir="rtl" style="text-align: right;">

### 🔬 Builder Pattern:

<div dir="ltr" style="text-align: left;">

```csharp
class PersonBuilder {
    private string name;
    private int age;
    
    public PersonBuilder WithName(string name) {
        this.name = name;
        return this;
    }
    
    // Overload برای convenience:
    public PersonBuilder WithName(string firstName, string lastName) {
        this.name = $"{firstName} {lastName}";
        return this;
    }
    
    public PersonBuilder WithAge(int age) {
        this.age = age;
        return this;
    }
    
    public Person Build() {
        return new Person(name, age);
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 Best Practices:

**1. از Overloading برای همان عمل استفاده کن:**

<div dir="ltr" style="text-align: left;">

```csharp
// ✅ خوب: همان عمل
class Math {
    public int Add(int a, int b) => a + b;
    public double Add(double a, double b) => a + b;
}

// ❌ بد: عمل متفاوت
class Processor {
    // public void Process(int x) { }
    // public void Process(string s) { }  // عمل متفاوته! از نام‌های مختلف استفاده کن
}
```

<div dir="rtl" style="text-align: right;">

**2. از Optional Parameters به جای Overloading استفاده کن:**

<div dir="ltr" style="text-align: left;">

```csharp
// ✅ خوب: Optional Parameters
class Test {
    public void Method(int a, int b = 0, int c = 0) { }
}

// ⚠️ بد: Overloading زیاد
class Test {
    public void Method(int a) { }
    public void Method(int a, int b) { }
    public void Method(int a, int b, int c) { }
}
```

<div dir="rtl" style="text-align: right;">

### ⚠️ Anti-Patterns:

**1. Overloading با Return Type:**

<div dir="ltr" style="text-align: left;">

```csharp
// ❌ نمی‌تونیم:
// public int Method() { return 1; }
// public string Method() { return "1"; }
```

<div dir="rtl" style="text-align: right;">

**2. Ambiguous Overloading:**

<div dir="ltr" style="text-align: left;">

```csharp
// ❌ Ambiguous:
class Test {
    public void Method(int x, double y) { }
    public void Method(double x, int y) { }
}
```

<div dir="rtl" style="text-align: right;">

### 💡 توصیه:

1. **از Overloading برای همان عمل استفاده کن**
2. **از Optional Parameters به جای Overloading زیاد استفاده کن**
3. **از نام‌های واضح استفاده کن** (اگر عمل متفاوته)
4. **Ambiguous Overloading رو اجتناب کن**

### 🎯 نتیجه:

Overload Resolution یک **قدرت** است، اما باید **درست** استفاده بشه. از Overloading برای **همان عمل با انواع مختلف** استفاده کن، نه برای **عمل‌های متفاوت**.

