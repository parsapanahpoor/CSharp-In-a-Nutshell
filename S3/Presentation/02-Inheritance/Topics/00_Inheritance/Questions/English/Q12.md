<div dir="ltr" style="text-align: left;">

## ❓ Q12:

## What is protected internal? How is it different from protected?

### ✅ Answer:

<div dir="ltr" style="text-align: left;">

```csharp
// Assembly A:
public class Base {
protected int A;              // Only Derived Classes
internal int B;               // Only this Assembly
protected internal int C;     // Derived Classes OR this Assembly ✅
private protected int D;      // Derived Classes in this Assembly ✅
}

// Assembly B:
public class Derived : Base {
void Test() {
A = 1;  // ✅ OK (protected)
B = 2;  // ❌ Error (internal)
C = 3;  // ✅ OK (protected internal)
D = 4;  // ❌ Error (private protected)
}
}

// Assembly A (same assembly):
public class SameAssemblyClass {
void Test() {
var obj = new Base();
obj.A = 1;  // ❌ Error
obj.B = 2;  // ✅ OK (internal)
obj.C = 3;  // ✅ OK (protected internal)
obj.D = 4;  // ❌ Error (not derived)
}
}

```

### 📊 Comparison Table:

| Access Modifier | Same Class | Derived (Same Assembly) | Derived (Other Assembly) | Same Assembly (Non-Derived) |
|----------------|-----------|------------------------|------------------------|----------------------------|
| **protected** | ✅ | ✅ | ✅ | ❌ |
| **internal** | ✅ | ✅ | ❌ | ✅ |
| **protected internal** | ✅ | ✅ | ✅ | ✅ |
| **private protected** | ✅ | ✅ | ❌ | ❌ |

<div dir="ltr" style="text-align: left;">

### 🎯 Key Points:

- **`protected internal`** = `protected` **OR** `internal` (broader)
- **`private protected`** = `protected` **AND** `internal` (narrower)
- **Usage:** For libraries that want to provide Extension Points
