<div dir="ltr" style="text-align: left;">

## ❓ Q13:

## Is Field Hiding dangerous? What problems does it create?

### 💡 Problem:

<div dir="ltr" style="text-align: left;">


```csharp
class Base {
public int Value = 10;
}

class Derived : Base {
public new int Value = 20;  // ⚠️ Field Hiding
}

Base obj1 = new Derived();
Console.WriteLine(obj1.Value);        // 10 😱

Derived obj2 = new Derived();
Console.WriteLine(obj2.Value);        // 20 😱

// One variable, two different values! 🤯

```
<div dir="ltr" style="text-align: left;">

### ✅ Deep Analysis:

<div dir="ltr" style="text-align: left;">


```csharp
class Base {
public int Value = 10;
}

class Derived : Base {
public new int Value = 20;
}

var obj = new Derived();

// Actual Memory Layout:
// [Derived Object]
// ├─ Base.Value = 10    ← obj as Base
// └─ Derived.Value = 20 ← obj as Derived

Console.WriteLine(obj.Value);              // 20 (Derived)
Console.WriteLine(((Base)obj).Value);      // 10 (Base) 😱
Console.WriteLine(obj.GetType().GetField("Value").GetValue(obj)); // 20

```

### 🎯 Best Practice:

```csharp
// ❌ Bad: Field Hiding
class Derived : Base {
public new int Value = 20;
}

// ✅ Good: Property Override
class Base {
public virtual int Value { get; set; } = 10;
}

class Derived : Base {
public override int Value { get; set; } = 20;
}

```

<div dir="ltr" style="text-align: left;">

### ⚠️ Dangers of Field Hiding:

- **Polymorphism breaks**: Reference type determines which value is returned
- **Hidden bugs**: Unexpected behavior at runtime
- **LSP violation**: Replacing Base with Derived is not safe
- **Maintenance hell**: Two fields with the same name in memory!

### 💡 Conclusion:

**Never** use Field Hiding! If you need to change behavior, use Property with `virtual`/`override`.
