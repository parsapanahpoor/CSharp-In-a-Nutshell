<div dir="ltr" style="text-align: left;">

## ❓ Q11:

## Why don't we have base.base? How to access GrandParent?

### 💡 Problem:

<div dir="ltr" style="text-align: left;">

```csharp
class GrandParent {
public virtual void Method() => Console.WriteLine("GrandParent");
}

class Parent : GrandParent {
public override void Method() => Console.WriteLine("Parent");
}

class Child : Parent {
public override void Method() {
base.Method();           // ✅ Calls Parent
base.base.Method();      // ❌ Syntax Error!

// How do I call GrandParent? 🤔
}
}

<div dir="ltr" style="text-align: left;">
```

### ✅ Answer:

<div dir="ltr" style="text-align: left;">

```csharp
// Solution 1: Use Parent
class Parent : GrandParent {
public override void Method() => Console.WriteLine("Parent");

protected void CallGrandParentMethod() {
base.Method(); // GrandParent
}
}

class Child : Parent {
public override void Method() {
CallGrandParentMethod(); // ✅ Indirect Access
}
}

// Solution 2: Template Method Pattern
class GrandParent {
public void Process() {
Step1();
Step2();
}
protected virtual void Step1() { }
protected virtual void Step2() { }
}

class Child : Parent {
protected override void Step1() {
// Only override this part
}
}
```

<div dir="ltr" style="text-align: left;">

### 🎯 Design Reason:

- We don't have `base.base` because it violates **Encapsulation**
- Each class should only communicate with its direct Parent
- If you need GrandParent → wrong design!
