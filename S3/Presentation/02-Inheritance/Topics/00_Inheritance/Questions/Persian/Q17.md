<div dir="rtl" style="text-align: right;">

## ❓ Q17:

## چرا C# فقط Single Inheritance داره و Multiple Inheritance چرا مجاز نیست؟

### ✅ جواب عمیق:

این یک **طراحی عمدی** برای جلوگیری از مشکلات **Diamond Problem** و پیچیدگی‌های Type System است.

### ❌ Diamond Problem (مشکل الماس):

<div dir="ltr" style="text-align: left;">

```csharp
// فرض کنیم Multiple Inheritance مجاز بود:
class A {
    public void Method() => Console.WriteLine("A");
}

class B : A {
    public override void Method() => Console.WriteLine("B");
}

class C : A {
    public override void Method() => Console.WriteLine("C");
}

class D : B, C {  // ❌ کدوم Method() رو باید استفاده کنه؟
    // D از B و C ارث می‌بره
    // B.Method() یا C.Method()؟
}

D d = new D();
d.Method();  // ❌ Ambiguous! کدوم؟
```

<div dir="rtl" style="text-align: right;">

### 🔍 مشکلات Multiple Inheritance:

**1. Ambiguity (ابهام):**
- اگر دو کلاس پایه متد یکسان داشته باشند، کدوم استفاده می‌شه؟

**2. Constructor Order:**
- ترتیب فراخوانی constructor ها چیه؟
- `B()` اول یا `C()` اول؟

**3. Virtual Method Resolution:**
- اگر `B` و `C` هر دو `Method()` رو override کرده باشند، کدوم اجرا می‌شه؟

**4. Memory Layout:**
- فیلدهای `B` و `C` چطور در memory چیده می‌شن؟
- اگر فیلد یکسان داشته باشند چی؟

<div dir="ltr" style="text-align: left;">

```csharp
class B {
    public int X = 10;
}

class C {
    public int X = 20;  // ❌ نام یکسان!
}

class D : B, C {  // D.X کدومه؟ 10 یا 20؟
}
```

<div dir="rtl" style="text-align: right;">

### ✅ راه حل C#: Interfaces

<div dir="ltr" style="text-align: left;">

```csharp
// به جای Multiple Inheritance:
interface IReadable {
    void Read();
}

interface IWritable {
    void Write();
}

class File : IReadable, IWritable {  // ✅ Multiple Interface Implementation
    public void Read() { }
    public void Write() { }
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 مزایای Single Inheritance:

1. **سادگی**: Type System ساده‌تر و قابل فهم‌تر
2. **Performance**: Resolution سریع‌تر (بدون ambiguity)
3. **Type Safety**: خطاهای کمتر در compile-time
4. **Maintainability**: کد راحت‌تر نگهداری می‌شه

### 💡 مقایسه با C++:

C++ Multiple Inheritance داره اما مشکلات زیادی داره:
- Virtual Inheritance برای حل Diamond Problem
- پیچیدگی در Constructor/Destructor order
- Ambiguity resolution با `::` operator

C# با **Single Inheritance + Multiple Interfaces** این مشکلات رو نداره!

