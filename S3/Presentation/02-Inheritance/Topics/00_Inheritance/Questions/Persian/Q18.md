<div dir="rtl" style="text-align: right;">

## ❓ Q18:

## System.Object و Inheritance Chain چطور کار می‌کنه؟

### ✅ جواب عمیق:

همه کلاس‌ها در C# **مستقیم یا غیرمستقیم** از `System.Object` ارث می‌برند.

### 🔍 Inheritance Chain:

<div dir="ltr" style="text-align: left;">

```csharp
class Animal { }  // : Object (implicit)
class Dog : Animal { }  // : Animal : Object
class Puppy : Dog { }  // : Dog : Animal : Object
```

<div dir="rtl" style="text-align: right;">

### 📊 Type Hierarchy:

<div dir="ltr" style="text-align: left;">

```
System.Object (Base)
    │
    ├─ GetType() → Type
    ├─ Equals(object) → bool
    ├─ GetHashCode() → int
    ├─ ToString() → string
    ├─ MemberwiseClone() → object
    └─ Finalize() → void
    │
    └─ Animal (Derived)
        │
        └─ Dog (Derived)
            │
            └─ Puppy (Derived)
```

<div dir="rtl" style="text-align: right;">

### 🔬 جزئیات System.Object Methods:

<div dir="ltr" style="text-align: left;">

```csharp
class Animal {
    // همه این‌ها از Object ارث برده شده:
    
    // 1. GetType() - نوع واقعی رو برمی‌گردونه
    public Type GetType() { }  // از Object
    
    // 2. Equals() - مقایسه equality
    public virtual bool Equals(object obj) { }  // از Object
    
    // 3. GetHashCode() - برای Hash Tables
    public virtual int GetHashCode() { }  // از Object
    
    // 4. ToString() - نمایش رشته‌ای
    public virtual string ToString() { }  // از Object
    
    // 5. MemberwiseClone() - shallow copy
    protected object MemberwiseClone() { }  // از Object
    
    // 6. Finalize() - destructor
    protected virtual void Finalize() { }  // از Object
}
```

<div dir="rtl" style="text-align: right;">

### ⚙️ Virtual Methods در Object:

<div dir="ltr" style="text-align: left;">

```csharp
class Animal {
    public override string ToString() => "Animal";
    public override bool Equals(object obj) {
        if (obj is Animal a) {
            return this.Name == a.Name;
        }
        return false;
    }
    public override int GetHashCode() {
        return Name?.GetHashCode() ?? 0;
    }
}

Animal a1 = new Animal { Name = "Dog" };
Animal a2 = new Animal { Name = "Dog" };

a1.Equals(a2);  // ✅ true (چون override کردیم)
a1.GetHashCode() == a2.GetHashCode();  // ✅ true
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکات پیشرفته:

1. **GetType()** virtual نیست - همیشه نوع واقعی رو برمی‌گردونه
2. **MemberwiseClone()** protected است - فقط از داخل کلاس قابل دسترسی
3. **Finalize()** protected virtual است - برای destructor استفاده می‌شه
4. **ReferenceEquals()** static method است - مقایسه reference

### ⚠️ مهم:

اگر `Equals()` رو override کنی، باید `GetHashCode()` رو هم override کنی!
در غیر این صورت Hash Tables درست کار نمی‌کنن.

