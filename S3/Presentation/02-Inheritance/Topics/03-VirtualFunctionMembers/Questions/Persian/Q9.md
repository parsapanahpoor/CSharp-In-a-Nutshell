<div dir="rtl" style="text-align: right;">

## ❓ Q9:

## چرا Virtual Methods Performance Overhead دارن؟

### ✅ جواب:

چون باید در **runtime** نوع واقعی شی رو چک کنن و متد درست رو صدا بزنن.

<div dir="ltr" style="text-align: left;">

```csharp
class Animal {
    public virtual void Speak() { }  // Virtual
    public void Eat() { }             // Non-virtual
}

Animal animal = new Dog();

// Virtual Call:
animal.Speak();  // ⚠️ باید VMT رو چک کنه (~2-3 CPU cycles)

// Direct Call:
animal.Eat();     // ✅ مستقیم به Animal.Eat می‌ره (~1 CPU cycle)
```

<div dir="rtl" style="text-align: right;">

### 📊 Performance Comparison:

| نوع | CPU Cycles | توضیح |
|-----|-----------|-------|
| **Non-virtual** | ~1 | Direct call |
| **Virtual** | ~2-3 | VMT lookup |

### 🎯 نکته:

این overhead **خیلی ناچیز** است و در 99% موارد مهم نیست. مزایای Polymorphism خیلی بیشتره!

### ⚠️ فقط در Hot Path ها:

اگر در یک حلقه خیلی تکراری هستی، ممکنه مهم باشه. در این صورت از `sealed override` استفاده کن.

