<div dir="rtl" style="text-align: right;">

## ❓ Q15:

## چرا Virtual Methods برای Design Patterns مهمن؟

### ✅ جواب:

بیشتر Design Patterns بر پایه Virtual Methods و Polymorphism هستند.

### مثال: Template Method Pattern

<div dir="ltr" style="text-align: left;">

```csharp
abstract class DataProcessor {
    // Template Method (با virtual methods):
    public void Process() {
        LoadData();      // Abstract
        ValidateData();  // Virtual (می‌تونه override بشه)
        TransformData(); // Abstract
        SaveData();      // Virtual (می‌تونه override بشه)
    }
    
    protected abstract void LoadData();
    protected virtual void ValidateData() {
        Console.WriteLine("Default validation");
    }
    protected abstract void TransformData();
    protected virtual void SaveData() {
        Console.WriteLine("Default save");
    }
}

class CsvProcessor : DataProcessor {
    protected override void LoadData() {
        Console.WriteLine("Loading CSV");
    }
    
    protected override void ValidateData() {
        base.ValidateData();  // ✅ Default validation
        Console.WriteLine("CSV-specific validation");
    }
    
    protected override void TransformData() {
        Console.WriteLine("Transforming CSV");
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

Virtual Methods به ما اجازه می‌دن **Template Method Pattern** رو پیاده کنیم: یک الگوریتم کلی با مراحل قابل تغییر.

