<div dir="rtl" style="text-align: right;">

## ❓ Q2:

## چرا به Virtual Methods نیاز داریم؟

### ✅ جواب:

بدون virtual، نمی‌تونیم رفتار کلاس پایه رو در کلاس مشتق تغییر بدیم.

<div dir="ltr" style="text-align: left;">

```csharp
// ❌ بدون virtual:
class Animal {
    public void Speak() {  // virtual نیست
        Console.WriteLine("Animal sound");
    }
}

class Dog : Animal {
    public void Speak() {  // این Hiding است، نه Override!
        Console.WriteLine("Woof!");
    }
}

Animal animal = new Dog();
animal.Speak();  // خروجی: "Animal sound" ❌ (نه "Woof!")
```

<div dir="rtl" style="text-align: right;">

### ✅ با virtual:

<div dir="ltr" style="text-align: left;">

```csharp
class Animal {
    public virtual void Speak() {  // ✅ virtual
        Console.WriteLine("Animal sound");
    }
}

class Dog : Animal {
    public override void Speak() {  // ✅ override
        Console.WriteLine("Woof!");
    }
}

Animal animal = new Dog();
animal.Speak();  // خروجی: "Woof!" ✅
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

Virtual methods به ما اجازه می‌دن **رفتار مشترک** رو تعریف کنیم و در کلاس‌های مشتق **تغییر** بدیم.

