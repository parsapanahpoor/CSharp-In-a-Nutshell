<div dir="rtl" style="text-align: right;">

## ❓ Q8:

## Virtual Property با Auto-Property چطور کار می‌کنه؟

### ✅ جواب:

می‌تونیم virtual property رو به صورت auto-property تعریف کنیم.

<div dir="ltr" style="text-align: left;">

```csharp
class Animal {
    public virtual string Name { get; set; }  // ✅ Virtual auto-property
}

class Dog : Animal {
    public override string Name { 
        get => base.Name;
        set => base.Name = value?.ToUpper();  // تبدیل به uppercase
    }
}

Animal animal = new Dog();
animal.Name = "buddy";
Console.WriteLine(animal.Name);  // "BUDDY" ✅
```

<div dir="rtl" style="text-align: right;">

### 🔍 یا می‌تونیم فقط getter رو override کنیم:

<div dir="ltr" style="text-align: left;">

```csharp
class Employee {
    public double BaseSalary { get; set; }
    public virtual double TotalSalary => BaseSalary;  // ✅ Virtual property
}

class Manager : Employee {
    public override double TotalSalary => BaseSalary * 1.5;  // ✅ فقط getter
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

می‌تونیم **getter** و **setter** رو جداگانه override کنیم.

