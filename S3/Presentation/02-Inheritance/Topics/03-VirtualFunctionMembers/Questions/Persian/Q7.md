<div dir="rtl" style="text-align: right;">

## ❓ Q7:

## Virtual Methods در Constructor چطور کار می‌کنن؟

### ⚠️ مشکل:

صدا زدن virtual method در constructor **خطرناک** است! چون کلاس مشتق هنوز initialize نشده.

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public Base() {
        Initialize();  // ⚠️ virtual method در constructor
    }
    
    protected virtual void Initialize() {
        Console.WriteLine("Base initialized");
    }
}

class Derived : Base {
    private int value = 10;  // هنوز initialize نشده!
    
    protected override void Initialize() {
        Console.WriteLine($"Derived initialized: {value}");
        // value = 0! ❌ (هنوز initialize نشده)
    }
}

new Derived();
// خروجی: "Derived initialized: 0" ❌
```

<div dir="rtl" style="text-align: right;">

### ✅ راه حل:

از virtual methods در constructor استفاده **نکن**! یا از Initialize pattern استفاده کن.

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public Base() {
        // virtual method صدا نزن!
    }
    
    public void Initialize() {  // بعد از construction
        DoInitialize();
    }
    
    protected virtual void DoInitialize() {
        Console.WriteLine("Base initialized");
    }
}

var obj = new Derived();
obj.Initialize();  // ✅ حالا همه fields initialize شدن
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

**هرگز** virtual method رو در constructor صدا نزن!

