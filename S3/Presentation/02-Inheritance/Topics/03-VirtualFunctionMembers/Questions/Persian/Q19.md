<div dir="rtl" style="text-align: right;">

## ❓ Q19:

## Sealed Override و Devirtualization Optimization چیه؟

### ✅ جواب عمیق:

`sealed override` به کامپایلر و JIT Compiler می‌گه که این متد دیگه override نمی‌شه و می‌تونن **Optimization** انجام بدن.

### 🔍 Sealed Override:

<div dir="ltr" style="text-align: left;">

```csharp
class Animal {
    public virtual void Speak() => Console.WriteLine("Animal");
}

class Dog : Animal {
    public sealed override void Speak() => Console.WriteLine("Dog");
    // ✅ دیگه نمی‌شه override کرد
}

class Puppy : Dog {
    // public override void Speak() { }  // ❌ Error: Cannot override sealed method
}
```

<div dir="rtl" style="text-align: right;">

### ⚡ Devirtualization Optimization:

<div dir="ltr" style="text-align: left;">

```csharp
// بدون sealed:
Animal animal = new Dog();
animal.Speak();  // Virtual Call (~2-3 cycles)

// با sealed override:
Animal animal2 = new Dog();
animal2.Speak();  // کامپایلر می‌تونه Direct Call بزنه (~1 cycle)
```

<div dir="rtl" style="text-align: right;">

### 🔬 JIT Optimization:

JIT Compiler می‌تونه در این موارد **Devirtualization** انجام بده:

1. **Sealed Override**: اگر متد sealed باشه
2. **Final Class**: اگر کلاس sealed باشه
3. **Type Analysis**: اگر JIT بتونه نوع رو در compile-time تشخیص بده

<div dir="ltr" style="text-align: left;">

```csharp
// JIT می‌تونه این کد:
Animal animal = new Dog();
animal.Speak();  // Virtual Call

// رو به این تبدیل کنه (اگر sealed باشه):
Dog dog = new Dog();
dog.Speak();  // Direct Call (Optimized!)
```

<div dir="rtl" style="text-align: right;">

### 📊 Performance Impact:

| حالت | Call Type | CPU Cycles | Optimization |
|------|-----------|------------|--------------|
| Virtual (بدون sealed) | Virtual Call | 2-3 | ❌ |
| Sealed Override | Direct Call | 1 | ✅ |
| Final Class | Direct Call | 1 | ✅ |

### 🎯 نکته پیشرفته:

این optimization در **Release mode** و با **Tiered JIT** فعال بیشتر موثره. در Debug mode ممکنه همیشه Virtual Call بزنه.

### ⚠️ Trade-off:

Sealed Override **Performance** رو بهتر می‌کنه اما **Extensibility** رو محدود می‌کنه. باید بین این دو تعادل برقرار کنی.

