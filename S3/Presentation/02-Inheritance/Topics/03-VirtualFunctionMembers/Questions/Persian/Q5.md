<div dir="rtl" style="text-align: right;">

## ❓ Q5:

## چرا متدهای static نمی‌تونن virtual باشن؟

### ✅ جواب:

چون **static methods** به کلاس تعلق دارن، نه به instance. Virtual methods برای **instance methods** هستند.

<div dir="ltr" style="text-align: left;">

```csharp
class Animal {
    // ❌ نمی‌تونیم این کار رو بکنیم:
    // public static virtual void Speak() { }
    
    // ✅ اما می‌تونیم instance virtual داشته باشیم:
    public virtual void Speak() { }
}
```

<div dir="rtl" style="text-align: right;">

### 🔍 دلیل:

- **Static methods** در compile-time resolve می‌شن
- **Virtual methods** در runtime resolve می‌شن
- این دو با هم **ناسازگار** هستند!

### 🎯 نکته:

Static methods **Polymorphism** ندارن. همیشه از کلاسی که صدا زده شدن اجرا می‌شن.

