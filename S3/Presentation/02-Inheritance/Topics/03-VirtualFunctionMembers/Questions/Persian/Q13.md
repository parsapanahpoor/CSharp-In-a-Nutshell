<div dir="rtl" style="text-align: right;">

## ❓ Q13:

## Virtual Methods با Events چطور کار می‌کنن؟

### ✅ جواب:

Events نمی‌تونن virtual باشن، اما می‌تونیم از **virtual methods** برای raise کردن events استفاده کنیم.

<div dir="ltr" style="text-align: left;">

```csharp
class Animal {
    public event EventHandler? Spoke;
    
    protected virtual void OnSpoke() {  // ✅ Virtual method
        Spoke?.Invoke(this, EventArgs.Empty);
    }
    
    public void Speak() {
        Console.WriteLine("Animal sound");
        OnSpoke();  // ✅ Virtual method صدا زده می‌شه
    }
}

class Dog : Animal {
    protected override void OnSpoke() {  // ✅ Override
        Console.WriteLine("Dog is about to speak...");
        base.OnSpoke();  // ✅ Base event رو raise می‌کنه
    }
}

var dog = new Dog();
dog.Spoke += (s, e) => Console.WriteLine("Event fired!");
dog.Speak();
// خروجی:
// Animal sound
// Dog is about to speak...
// Event fired!
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

این pattern برای **Custom Event Raising** استفاده می‌شه. به کلاس‌های مشتق اجازه می‌ده قبل/بعد از event رفتار اضافه کنن.

