<div dir="rtl" style="text-align: right;">

## ❓ Q12:

## Virtual Methods با Abstract Methods چه تفاوتی دارن؟

### ✅ مقایسه:

| ویژگی | Virtual | Abstract |
|-------|---------|----------|
| **Implementation** | باید داشته باشه | نباید داشته باشه |
| **کلاس** | می‌تونه concrete باشه | باید abstract باشه |
| **Override** | اختیاری | اجباری |
| **استفاده** | Default behavior | Contract |

<div dir="ltr" style="text-align: left;">

```csharp
// Virtual (با implementation):
class Animal {
    public virtual void Speak() {
        Console.WriteLine("Animal sound");  // ✅ Default implementation
    }
}

// Abstract (بدون implementation):
abstract class Animal {
    public abstract void Speak();  // ❌ باید در Derived implement بشه
}

class Dog : Animal {
    public override void Speak() {  // ✅ اجباری است
        Console.WriteLine("Woof!");
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

**Virtual** = Default behavior (می‌تونه override بشه)
**Abstract** = Contract (باید implement بشه)

