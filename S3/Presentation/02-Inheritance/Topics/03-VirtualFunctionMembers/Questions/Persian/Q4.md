<div dir="rtl" style="text-align: right;">

## ❓ Q4:

## base keyword چیه؟

### ✅ جواب:

`base` keyword به ما اجازه می‌ده به **متد یا property کلاس پایه** دسترسی داشته باشیم.

<div dir="ltr" style="text-align: left;">

```csharp
class Printer {
    public virtual void Print(string text) {
        Console.WriteLine($"[Base] Printing: {text}");
    }
}

class AdvancedPrinter : Printer {
    public override void Print(string text) {
        Console.WriteLine("[Advanced] Pre-processing...");
        base.Print(text);  // ✅ صدا زدن متد پایه
        Console.WriteLine("[Advanced] Post-processing...");
    }
}

var printer = new AdvancedPrinter();
printer.Print("Hello");
// خروجی:
// [Advanced] Pre-processing...
// [Base] Printing: Hello
// [Advanced] Post-processing...
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

`base` فقط برای **override methods** کار می‌کنه. نمی‌تونیم از base برای متدهای عادی استفاده کنیم.

