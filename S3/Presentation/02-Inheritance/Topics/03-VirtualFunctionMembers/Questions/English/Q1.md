<div dir="ltr" style="text-align: left;">

## ❓ Q1:

## What is Virtual Method?

### ✅ Answer:

**Virtual Method** is a method marked with `virtual` keyword that can be **overridden** in derived classes.

<div dir="ltr" style="text-align: left;">

```csharp
class Animal {
    public virtual void Speak() {
        Console.WriteLine("Animal makes sound");
    }
}

class Dog : Animal {
    public override void Speak() {
        Console.WriteLine("Woof!");
    }
}

Animal animal = new Dog();
animal.Speak();  // Output: "Woof!" ✅
```

<div dir="ltr" style="text-align: left;">

### 🎯 Note:

Virtual methods enable **Runtime Polymorphism**. The decision is made at runtime.
