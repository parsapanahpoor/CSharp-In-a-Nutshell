<div dir="ltr" style="text-align: left;">

## ❓ Q8:

## How do Virtual Property work with Auto-Property?

### ✅ Answer:

We can define virtual property as auto-property.

<div dir="ltr" style="text-align: left;">

```csharp
class Animal {
    public virtual string Name { get; set; }  // ✅ Virtual auto-property
}

class Dog : Animal {
    public override string Name { 
        get => base.Name;
        set => base.Name = value?.ToUpper();  // Convert to uppercase
    }
}

Animal animal = new Dog();
animal.Name = "buddy";
Console.WriteLine(animal.Name);  // "BUDDY" ✅
```

<div dir="ltr" style="text-align: left;">

### 🔍 Or we can override only getter:

<div dir="ltr" style="text-align: left;">

```csharp
class Employee {
    public double BaseSalary { get; set; }
    public virtual double TotalSalary => BaseSalary;  // ✅ Virtual property
}

class Manager : Employee {
    public override double TotalSalary => BaseSalary * 1.5;  // ✅ Only getter
}
```

<div dir="ltr" style="text-align: left;">

### 🎯 Note:

We can override **getter** and **setter** separately.
