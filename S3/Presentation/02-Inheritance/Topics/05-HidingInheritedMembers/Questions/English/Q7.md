<div dir="ltr" style="text-align: left;">

## ❓ Q7:

## How does Hiding Field work?

### ✅ Answer:

Fields can also be hidden, but it's **very dangerous**!

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public int value = 10;
}

class Derived : Base {
    public new int value = 20;  // ✅ Hiding field
}

Base b = new Derived();
Console.WriteLine(b.value);  // 10 ✅ (Base field)

Derived d = new Derived();
Console.WriteLine(d.value);  // 20 ✅ (Derived field)
```

<div dir="ltr" style="text-align: left;">

### ⚠️ Problem:

<div dir="ltr" style="text-align: left;">

```csharp
// This code has a problem:
Base obj = new Derived();
obj.value = 30;  // Base.value changes
Console.WriteLine(((Derived)obj).value);  // 20! ❌ (Derived.value didn't change!)
```

<div dir="ltr" style="text-align: left;">

### 🎯 Note:

**Never do Field Hiding**! Use Properties and virtual/override.
