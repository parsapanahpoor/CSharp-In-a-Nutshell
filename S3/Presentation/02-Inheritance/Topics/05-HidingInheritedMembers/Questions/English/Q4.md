<div dir="ltr" style="text-align: left;">

## ❓ Q4:

## How does Hiding Property work?

### ✅ Answer:

Properties can also be hidden, like Methods.

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public int Value => 10;
}

class Derived : Base {
    public new int Value => 20;  // ✅ Hiding property
}

Base b = new Derived();
Console.WriteLine(b.Value);  // 10 ✅ (Base property)

Derived d = new Derived();
Console.WriteLine(d.Value);  // 20 ✅ (Derived property)
```

<div dir="ltr" style="text-align: left;">

### 🎯 Note:

Hiding Property also **doesn't have Polymorphism**. Decision is made based on reference type.
