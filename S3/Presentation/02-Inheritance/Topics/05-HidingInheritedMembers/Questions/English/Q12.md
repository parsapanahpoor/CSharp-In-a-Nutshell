<div dir="ltr" style="text-align: left;">

## ❓ Q12:

## How does Hiding work with Static Methods?

### ✅ Answer:

Static Methods can also be hidden, but since they're Static, they **don't have any Polymorphism**.

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public static void Method() {
        Console.WriteLine("Base");
    }
}

class Derived : Base {
    public new static void Method() {  // ✅ Hiding static
        Console.WriteLine("Derived");
    }
}

Base.Method();   // "Base" ✅
Derived.Method(); // "Derived" ✅
```

<div dir="ltr" style="text-align: left;">

### 🎯 Note:

Static Methods **don't have any Polymorphism** (even with Override). Hiding only changes the name.
