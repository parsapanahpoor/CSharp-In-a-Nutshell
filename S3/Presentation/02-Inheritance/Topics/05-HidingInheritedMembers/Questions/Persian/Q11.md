<div dir="rtl" style="text-align: right;">

## ❓ Q11:

## می‌تونیم Base Member رو بعد از Hiding دسترسی داشته باشیم؟

### ✅ جواب:

بله! با **base** keyword می‌تونیم به Base Member دسترسی داشته باشیم.

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public void Method() {
        Console.WriteLine("Base");
    }
}

class Derived : Base {
    public new void Method() {
        base.Method();  // ✅ Base method رو صدا می‌زنیم
        Console.WriteLine("Derived");
    }
}

Derived d = new Derived();
d.Method();
// خروجی:
// Base
// Derived
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

`base` keyword به ما اجازه می‌ده به **Base Member** دسترسی داشته باشیم، حتی بعد از Hiding.

