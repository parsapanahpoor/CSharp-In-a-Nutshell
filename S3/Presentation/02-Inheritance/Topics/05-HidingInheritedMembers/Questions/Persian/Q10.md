<div dir="rtl" style="text-align: right;">

## ❓ Q10:

## چرا Hiding Polymorphism نداره؟

### ✅ جواب:

چون Hiding در **Compile-time** resolve می‌شه، نه Runtime. کامپایلر بر اساس **نوع رفرنس** تصمیم می‌گیره.

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public void Method() {
        Console.WriteLine("Base");
    }
}

class Derived : Base {
    public new void Method() {
        Console.WriteLine("Derived");
    }
}

Base obj = new Derived();
obj.Method();  // کامپایلر می‌بینه: obj نوع Base است
               // پس Base.Method() رو صدا می‌زنه ✅
               // نه Derived.Method() ❌
```

<div dir="rtl" style="text-align: right;">

### 🔍 مقایسه با Override:

<div dir="ltr" style="text-align: left;">

```csharp
// با Override (Runtime):
class Base {
    public virtual void Method() { }
}

class Derived : Base {
    public override void Method() { }
}

Base obj = new Derived();
obj.Method();  // Runtime نوع واقعی رو چک می‌کنه: Derived
               // پس Derived.Method() رو صدا می‌زنه ✅
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

**Hiding** = Compile-time (نوع رفرنس) | **Override** = Runtime (نوع واقعی)

