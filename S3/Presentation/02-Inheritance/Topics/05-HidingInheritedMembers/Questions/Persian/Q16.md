<div dir="rtl" style="text-align: right;">

## ❓ Q16:

## Member Resolution و Method Lookup در Hiding چطور کار می‌کنه؟

### ✅ جواب عمیق:

کامپایلر C# یک **الگوریتم پیچیده** برای Member Resolution داره که در Hiding استفاده می‌شه.

### 🔍 Member Lookup Algorithm:

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public void Method() { }
}

class Derived : Base {
    public new void Method() { }
}

Base obj = new Derived();
obj.Method();  // چه اتفاقی می‌افته؟
```

<div dir="rtl" style="text-align: right;">

### 📊 Resolution Process:

**مرحله 1: Type Resolution**
- کامپایلر نوع رفرنس رو می‌بینه: `Base`
- به Base Class می‌ره

**مرحله 2: Member Lookup**
- در Base Class دنبال `Method()` می‌گرده
- پیدا می‌کنه: `Base.Method()`

**مرحله 3: Hiding Check**
- بررسی می‌کنه که آیا Derived یک member با همین نام داره
- بله داره، اما چون رفرنس `Base` است، `Base.Method()` استفاده می‌شه

**نتیجه**: `Base.Method()` صدا زده می‌شه ✅

<div dir="ltr" style="text-align: left;">

```csharp
// اما:
Derived obj2 = new Derived();
obj2.Method();  // Derived.Method() صدا زده می‌شه ✅
```

<div dir="rtl" style="text-align: right;">

### 🔬 تفاوت با Override:

<div dir="ltr" style="text-align: left;">

```csharp
// با Override (Runtime Resolution):
class Base {
    public virtual void Method() { }
}

class Derived : Base {
    public override void Method() { }
}

Base obj = new Derived();
obj.Method();  // Runtime نوع واقعی رو چک می‌کنه: Derived
               // پس Derived.Method() رو صدا می‌زنه ✅
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته پیشرفته:

**Hiding** = Compile-time Member Lookup (بر اساس نوع رفرنس)
**Override** = Runtime Method Dispatch (بر اساس نوع واقعی)

### ⚙️ Performance:

- **Hiding**: ~0 overhead (Compile-time resolution)
- **Override**: ~2-3 CPU cycles (Runtime VMT lookup)

