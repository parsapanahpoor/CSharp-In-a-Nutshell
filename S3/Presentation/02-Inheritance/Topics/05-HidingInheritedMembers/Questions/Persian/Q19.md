<div dir="rtl" style="text-align: right;">

## ❓ Q19:

## Hiding با Reflection و Runtime Type Information چطور کار می‌کنه؟

### ✅ جواب عمیق:

Reflection می‌تونه Hidden Members رو در runtime بررسی کنه و به Base Members دسترسی داشته باشه.

### 🔍 Reflection برای Hidden Members:

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public void Method() {
        Console.WriteLine("Base");
    }
}

class Derived : Base {
    public new void Method() {
        Console.WriteLine("Derived");
    }
}

// Reflection:
Type baseType = typeof(Base);
Type derivedType = typeof(Derived);

// بررسی Methods:
MethodInfo[] baseMethods = baseType.GetMethods();
MethodInfo[] derivedMethods = derivedType.GetMethods();

// پیدا کردن Hidden Method:
MethodInfo baseMethod = baseType.GetMethod("Method");
MethodInfo derivedMethod = derivedType.GetMethod("Method");

// بررسی Hiding:
bool isHiding = derivedMethod.GetBaseDefinition() != derivedMethod;
Console.WriteLine($"Is Hiding: {isHiding}");  // True ✅
```

<div dir="rtl" style="text-align: right;">

### 🔬 Invoking Hidden Methods:

<div dir="ltr" style="text-align: left;">

```csharp
Derived obj = new Derived();

// با Reflection می‌تونیم Base Method رو صدا بزنیم:
MethodInfo baseMethod = typeof(Base).GetMethod("Method");
baseMethod.Invoke(obj, null);  // "Base" ✅

// یا Derived Method:
MethodInfo derivedMethod = typeof(Derived).GetMethod("Method");
derivedMethod.Invoke(obj, null);  // "Derived" ✅
```

<div dir="rtl" style="text-align: right;">

### 🔍 GetBaseDefinition():

<div dir="ltr" style="text-align: left;">

```csharp
MethodInfo method = typeof(Derived).GetMethod("Method");
MethodInfo baseDefinition = method.GetBaseDefinition();

Console.WriteLine(baseDefinition.DeclaringType);  // Base ✅
// GetBaseDefinition() به Base Method اشاره می‌کنه
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته پیشرفته:

Reflection می‌تونه **Hidden Members** رو پیدا کنه و به **Base Members** دسترسی داشته باشه. این برای **Code Analysis** و **Testing** مفیده.

### ⚠️ Performance:

Reflection برای Hidden Members **خیلی کند** است (~1000-10000 CPU cycles). فقط برای **Inspection** استفاده کن، نه برای **Invocation**!

### 💡 استفاده:

- **Code Analysis**: بررسی Hidden Members
- **Testing**: تست Base و Derived Methods
- **Debugging**: پیدا کردن Hidden Members

