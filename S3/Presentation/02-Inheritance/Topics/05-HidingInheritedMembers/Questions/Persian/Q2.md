<div dir="rtl" style="text-align: right;">

## ❓ Q2:

## تفاوت Hiding با Override چیه؟

### ✅ مقایسه:

| ویژگی | Hiding (`new`) | Override |
|-------|---------------|----------|
| **Polymorphism** | ❌ ندارد | ✅ دارد |
| **Virtual** | نیاز نداره | باید virtual باشه |
| **Runtime** | Compile-time | Runtime |
| **Base Reference** | Base method صدا می‌شه | Derived method صدا می‌شه |

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public void Method() { }  // Non-virtual
}

class Derived : Base {
    public new void Method() { }  // ✅ Hiding (Compile-time)
}

Base b = new Derived();
b.Method();  // Base.Method() صدا می‌شه ❌

// با Override:
class Base {
    public virtual void Method() { }
}

class Derived : Base {
    public override void Method() { }  // ✅ Override (Runtime)
}

Base b2 = new Derived();
b2.Method();  // Derived.Method() صدا می‌شه ✅
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

**Hiding** = Compile-time (بر اساس نوع رفرنس) | **Override** = Runtime (بر اساس نوع واقعی)

