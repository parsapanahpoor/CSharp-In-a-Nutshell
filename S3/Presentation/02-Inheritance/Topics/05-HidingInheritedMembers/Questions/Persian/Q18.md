<div dir="rtl" style="text-align: right;">

## ❓ Q18:

## Field Hiding و Memory Layout - چرا خطرناکه؟

### ✅ جواب عمیق:

Field Hiding **خیلی خطرناک** است چون دو field جداگانه در memory ساخته می‌شن که می‌تونه باعث bug بشه.

### 🔍 Memory Layout:

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public int value = 10;
}

class Derived : Base {
    public new int value = 20;  // ⚠️ Hiding field
}
```

<div dir="rtl" style="text-align: right;">

### 📊 Memory Structure:

<div dir="ltr" style="text-align: left;">

```
Derived Object in Heap:
┌─────────────────────────┐
│ Object Header           │
├─────────────────────────┤
│ Type Handle             │
├─────────────────────────┤
│ Base.value (int) = 10   │ ← Base field
├─────────────────────────┤
│ Derived.value (int) = 20│ ← Derived field (جداگانه!)
└─────────────────────────┘
```

<div dir="rtl" style="text-align: right;">

### ⚠️ مشکل:

<div dir="ltr" style="text-align: left;">

```csharp
Derived obj = new Derived();

// این دو به دو field مختلف اشاره می‌کنن:
Base b = obj;
b.value = 30;  // Base.value = 30 ✅

Console.WriteLine(obj.value);  // 20! ❌ (Derived.value تغییر نکرده!)
Console.WriteLine(((Base)obj).value);  // 30 ✅ (Base.value)
```

<div dir="rtl" style="text-align: right;">

### 🔬 چرا خطرناکه:

1. **دو Field جداگانه**: Base.value و Derived.value دو field مختلف هستند
2. **Memory Waste**: دو field در memory نگه داشته می‌شن
3. **Confusion**: کد خوانا نیست و باعث bug می‌شه
4. **No Polymorphism**: بر اساس نوع رفرنس تصمیم گرفته می‌شه

### ✅ راه حل:

<div dir="ltr" style="text-align: left;">

```csharp
// به جای Field Hiding:
class Base {
    public virtual int Value { get; set; } = 10;  // ✅ Property
}

class Derived : Base {
    public override int Value {  // ✅ Override
        get => base.Value * 2;
        set => base.Value = value / 2;
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته پیشرفته:

**هرگز Field Hiding نکن**! از Properties با virtual/override استفاده کن. Field Hiding فقط باعث confusion و bug می‌شه.

### ⚠️ Performance:

Field Hiding **Memory Overhead** داره (دو field در memory). Properties هم overhead دارن اما خیلی کمتر و قابل قبول‌ترن.

