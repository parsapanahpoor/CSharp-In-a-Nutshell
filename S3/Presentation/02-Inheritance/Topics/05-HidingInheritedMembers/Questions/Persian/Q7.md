<div dir="rtl" style="text-align: right;">

## ❓ Q7:

## Hiding Field چطور کار می‌کنه؟

### ✅ جواب:

Fields هم می‌تونن hide بشن، اما **خیلی خطرناک** است!

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public int value = 10;
}

class Derived : Base {
    public new int value = 20;  // ✅ Hiding field
}

Base b = new Derived();
Console.WriteLine(b.value);  // 10 ✅ (Base field)

Derived d = new Derived();
Console.WriteLine(d.value);  // 20 ✅ (Derived field)
```

<div dir="rtl" style="text-align: right;">

### ⚠️ مشکل:

<div dir="ltr" style="text-align: left;">

```csharp
// این کد مشکل داره:
Base obj = new Derived();
obj.value = 30;  // Base.value تغییر می‌کنه
Console.WriteLine(((Derived)obj).value);  // 20! ❌ (Derived.value تغییر نکرده!)
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

**هرگز Field Hiding نکن**! از Properties استفاده کن و virtual/override کن.

