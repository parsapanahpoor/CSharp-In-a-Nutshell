<div dir="rtl" style="text-align: right;">

## ❓ Q4:

## Hiding Property چطور کار می‌کنه؟

### ✅ جواب:

Properties هم می‌تونن hide بشن، مثل Methods.

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public int Value => 10;
}

class Derived : Base {
    public new int Value => 20;  // ✅ Hiding property
}

Base b = new Derived();
Console.WriteLine(b.Value);  // 10 ✅ (Base property)

Derived d = new Derived();
Console.WriteLine(d.Value);  // 20 ✅ (Derived property)
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

Hiding Property هم **Polymorphism نداره**. بر اساس نوع رفرنس تصمیم گرفته می‌شه.

