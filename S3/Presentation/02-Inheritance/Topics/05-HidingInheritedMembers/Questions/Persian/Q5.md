<div dir="rtl" style="text-align: right;">

## ❓ Q5:

## چرا Hiding خطرناکه؟

### ⚠️ مشکل:

Hiding می‌تونه باعث **سردرگمی** بشه چون Polymorphism نداره و رفتار غیرمنتظره ایجاد می‌کنه.

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public void Process() {
        Console.WriteLine("Base Process");
    }
}

class Derived : Base {
    public new void Process() {
        Console.WriteLine("Derived Process");
    }
}

Base obj = new Derived();
obj.Process();  // خروجی: "Base Process" ❌ (نه "Derived Process"!)

// این رفتار غیرمنتظره است!
```

<div dir="rtl" style="text-align: right;">

### ✅ راه حل:

از **virtual/override** استفاده کن:

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public virtual void Process() {
        Console.WriteLine("Base Process");
    }
}

class Derived : Base {
    public override void Process() {
        Console.WriteLine("Derived Process");
    }
}

Base obj = new Derived();
obj.Process();  // خروجی: "Derived Process" ✅
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

**تا جایی که ممکنه از virtual/override استفاده کن**! Hiding فقط در موارد خاص لازمه.

