<div dir="rtl" style="text-align: right;">

## ❓ Q6:

## چه موقع باید از Hiding استفاده کنیم؟

### ✅ جواب:

فقط وقتی که **واقعاً لازمه** و نمی‌تونیم از virtual/override استفاده کنیم.

### مثال: وقتی Base Class از یک Library است

<div dir="ltr" style="text-align: left;">

```csharp
// فرض کنیم Base از یک Library است و virtual نیست:
class Base {  // از Library
    public void Method() { }  // Virtual نیست!
}

// ما نمی‌تونیم Base رو تغییر بدیم:
class Derived : Base {
    public new void Method() {  // ✅ باید hide کنیم
        // Custom implementation
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 موارد استفاده:

1. وقتی Base Class از **Third-party Library** است
2. وقتی Base Class **virtual نیست** و نمی‌تونیم تغییرش بدیم
3. وقتی می‌خوایم **Backward Compatibility** حفظ کنیم

### ⚠️ توصیه:

**تا جایی که ممکنه از virtual/override استفاده کن**! Hiding آخرین راه حل است.

