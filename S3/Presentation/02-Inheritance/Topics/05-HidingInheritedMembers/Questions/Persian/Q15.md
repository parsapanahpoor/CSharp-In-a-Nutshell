<div dir="rtl" style="text-align: right;">

## ❓ Q15:

## Hiding با Indexers چطور کار می‌کنه؟

### ✅ جواب:

Indexers هم می‌تونن hide بشن، مثل Methods و Properties.

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    protected int[] items = new int[10];
    
    public virtual int this[int index] {
        get => items[index];
        set => items[index] = value;
    }
}

class Derived : Base {
    public new int this[int index] {  // ✅ Hiding indexer
        get => items[index] * 2;
        set => items[index] = value / 2;
    }
}

Base b = new Derived();
b[0] = 10;  // Base indexer ✅

Derived d = new Derived();
d[0] = 10;  // Derived indexer (value = 5) ✅
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

Indexer Hiding هم **Polymorphism نداره**. بر اساس نوع رفرنس تصمیم گرفته می‌شه.

