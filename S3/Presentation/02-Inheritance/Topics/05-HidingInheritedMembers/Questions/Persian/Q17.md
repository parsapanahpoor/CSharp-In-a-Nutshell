<div dir="rtl" style="text-align: right;">

## ❓ Q17:

## Hiding با Generic Types و Type Parameters چطور کار می‌کنه؟

### ✅ جواب عمیق:

Hiding در Generic Classes می‌تونه خیلی پیچیده بشه، مخصوصاً وقتی با Type Constraints ترکیب می‌شه.

### 🔍 مثال پیچیده:

<div dir="ltr" style="text-align: left;">

```csharp
class Base<T> {
    public void Process(T item) {
        Console.WriteLine($"Base processing {typeof(T).Name}");
    }
}

class Derived<T> : Base<T> {
    public new void Process(T item) {  // ✅ Hiding
        Console.WriteLine($"Derived processing {typeof(T).Name}");
    }
}

// استفاده:
Base<int> b = new Derived<int>();
b.Process(42);  // "Base processing Int32" ✅

Derived<int> d = new Derived<int>();
d.Process(42);  // "Derived processing Int32" ✅
```

<div dir="rtl" style="text-align: right;">

### ⚠️ مشکل با Covariance:

<div dir="ltr" style="text-align: left;">

```csharp
class Animal { }
class Dog : Animal { }

class Base<T> {
    public void Process(T item) { }
}

class Derived<T> : Base<T> {
    public new void Process(T item) { }
}

// ❌ نمی‌تونیم این کار رو بکنیم:
// Base<Animal> b = new Derived<Dog>();  // Error: Generic types invariant

// اما با Interface Covariance:
interface IProcessor<out T> {
    void Process(T item);
}

class Processor<T> : IProcessor<T> {
    public void Process(T item) { }
}

// حالا Covariance کار می‌کنه:
IProcessor<Animal> p = new Processor<Dog>();  // ✅
```

<div dir="rtl" style="text-align: right;">

### 🔬 Hiding با Type Constraints:

<div dir="ltr" style="text-align: left;">

```csharp
class Animal { }
class Dog : Animal { }

class Base<T> where T : Animal {
    public void Process(T animal) {
        animal.Eat();  // می‌دونه T : Animal است
    }
}

class Derived<T> : Base<T> where T : Animal {
    public new void Process(T animal) {
        base.Process(animal);  // ✅ Base method
        // می‌تونیم متدهای Animal رو صدا بزنیم
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته پیشرفته:

Hiding در Generic Classes **Type Safety** رو حفظ می‌کنه. کامپایلر بررسی می‌کنه که Type Constraints درست باشن.

### ⚠️ محدودیت:

Hiding نمی‌تونه Type Constraints رو تغییر بده. باید همان Constraints رو داشته باشه.

