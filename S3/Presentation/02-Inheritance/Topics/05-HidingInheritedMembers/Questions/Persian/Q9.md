<div dir="rtl" style="text-align: right;">

## ❓ Q9:

## Hiding Event چطور کار می‌کنه؟

### ✅ جواب:

Events هم می‌تونن hide بشن، اما **خیلی نادر** است.

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public event EventHandler? Clicked;
}

class Derived : Base {
    public new event EventHandler? Clicked;  // ✅ Hiding event
}

Base b = new Derived();
b.Clicked += (s, e) => Console.WriteLine("Base");  // Base event

Derived d = new Derived();
d.Clicked += (s, e) => Console.WriteLine("Derived");  // Derived event
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته:

Event Hiding **خیلی نادر** است و معمولاً لازم نیست. از virtual events استفاده کن.

