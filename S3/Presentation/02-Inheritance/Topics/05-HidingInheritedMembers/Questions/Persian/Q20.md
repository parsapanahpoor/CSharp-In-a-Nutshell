<div dir="rtl" style="text-align: right;">

## ❓ Q20:

## Hiding با Design Patterns و Best Practices چطور کار می‌کنه؟

### ✅ جواب عمیق:

Hiding معمولاً **Anti-pattern** است و باید ازش اجتناب کنیم. اما در بعضی موارد خاص لازمه.

### ⚠️ Anti-Pattern: Hiding

<div dir="ltr" style="text-align: left;">

```csharp
// ❌ بد: Hiding برای Polymorphism
class Base {
    public void Process() {
        Console.WriteLine("Base");
    }
}

class Derived : Base {
    public new void Process() {  // ❌ Hiding
        Console.WriteLine("Derived");
    }
}

Base obj = new Derived();
obj.Process();  // "Base" ❌ (نه "Derived"!)
```

<div dir="rtl" style="text-align: right;">

### ✅ Best Practice: Virtual/Override

<div dir="ltr" style="text-align: left;">

```csharp
// ✅ خوب: Virtual/Override برای Polymorphism
class Base {
    public virtual void Process() {
        Console.WriteLine("Base");
    }
}

class Derived : Base {
    public override void Process() {  // ✅ Override
        Console.WriteLine("Derived");
    }
}

Base obj = new Derived();
obj.Process();  // "Derived" ✅
```

<div dir="rtl" style="text-align: right;">

### 🔬 موارد استفاده صحیح Hiding:

**1. Third-party Library Integration:**

<div dir="ltr" style="text-align: left;">

```csharp
// Base از Library است و virtual نیست:
class ThirdPartyBase {  // از Library
    public void Method() { }  // Virtual نیست!
}

// ما نمی‌تونیم تغییرش بدیم:
class MyDerived : ThirdPartyBase {
    public new void Method() {  // ✅ Hiding لازمه
        // Custom implementation
    }
}
```

<div dir="rtl" style="text-align: right;">

**2. Backward Compatibility:**

<div dir="ltr" style="text-align: left;">

```csharp
// Version 1:
class Base {
    public void OldMethod() { }
}

// Version 2: می‌خوایم OldMethod رو deprecate کنیم:
class Base {
    [Obsolete("Use NewMethod instead")]
    public void OldMethod() { }
    
    public void NewMethod() { }
}

// اما برای Backward Compatibility:
class Derived : Base {
    public new void OldMethod() {  // ✅ Hiding برای compatibility
        NewMethod();  // به NewMethod delegate می‌کنه
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🔬 Design Patterns:

**Adapter Pattern با Hiding:**

<div dir="ltr" style="text-align: left;">

```csharp
// وقتی می‌خوایم یک Interface رو adapt کنیم:
interface ILegacyInterface {
    void OldMethod();
}

class LegacyImplementation : ILegacyInterface {
    public void OldMethod() { }
}

class ModernAdapter : LegacyImplementation {
    public new void OldMethod() {  // ✅ Hiding برای Adapter
        // Modern implementation
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 Best Practices:

1. **تا جایی که ممکنه از virtual/override استفاده کن**
2. **Hiding فقط برای Third-party Libraries یا Backward Compatibility**
3. **هرگز Field Hiding نکن** - از Properties استفاده کن
4. **Documentation**: همیشه توضیح بده چرا Hiding استفاده کردی
5. **Testing**: Hidden Members رو تست کن

### ⚠️ Trade-offs:

- **Hiding**: Compile-time (سریع‌تر)، اما Polymorphism نداره
- **Override**: Runtime (کمی کندتر)، اما Polymorphism داره

### 💡 توصیه:

**99% مواقع از virtual/override استفاده کن**! Hiding فقط در موارد خاص لازمه.

