<div dir="rtl" style="text-align: right;">

## ❓ Q18:

## Sealed Classes با Generic Types و Type Constraints چطور کار می‌کنن؟

### ✅ جواب عمیق:

Sealed Generic Classes می‌تونن خیلی پیچیده بشن، مخصوصاً وقتی با Type Constraints ترکیب می‌شن.

### 🔍 Sealed Generic Class:

<div dir="ltr" style="text-align: left;">

```csharp
sealed class Container<T> where T : Animal {
    private T item;
    
    public Container(T item) {
        this.item = item;
    }
    
    public void Process() {
        item.Eat();  // می‌دونه T : Animal است
    }
}

// استفاده:
var container = new Container<Dog>(new Dog());
container.Process();  // ✅
```

<div dir="rtl" style="text-align: right;">

### ⚠️ محدودیت‌ها:

<div dir="ltr" style="text-align: left;">

```csharp
// ❌ نمی‌تونیم این کارها رو بکنیم:

// 1. Sealed Abstract Generic:
// abstract sealed class Base<T> { }  // Error!

// 2. Sealed با Covariance:
sealed class Container<T> { }
// Container<Animal> c = new Container<Dog>();  // ❌ Error: Generic types invariant
```

<div dir="rtl" style="text-align: right;">

### 🔬 Sealed Methods در Generic Classes:

<div dir="ltr" style="text-align: left;">

```csharp
class Base<T> {
    public virtual void Process(T item) { }
}

sealed class Derived<T> : Base<T> {
    public sealed override void Process(T item) {  // ✅ Sealed override
        // Final implementation
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته پیشرفته:

Sealed Generic Classes **Type Safety** رو حفظ می‌کنن و نمی‌تونن extend بشن. این برای **Value Types** و **Immutable Types** مفیده.

