<div dir="rtl" style="text-align: right;">

## ❓ Q16:

## Devirtualization و JIT Optimization با Sealed چطور کار می‌کنه؟

### ✅ جواب عمیق:

JIT Compiler می‌تونه با Sealed Classes و Methods **Devirtualization** انجام بده و Virtual Call رو به Direct Call تبدیل کنه.

### 🔍 Devirtualization Process:

<div dir="ltr" style="text-align: left;">

```csharp
class Base {
    public virtual void Method() { }
}

sealed class Derived : Base {
    public override void Method() { }
}

Base obj = new Derived();
obj.Method();  // چه اتفاقی می‌افته؟
```

<div dir="rtl" style="text-align: right;">

### ⚙️ JIT Optimization:

**بدون Sealed:**
- Virtual Call → VMT Lookup → ~2-3 CPU cycles

**با Sealed:**
- JIT می‌بینه که Derived sealed است
- می‌دونه که Method دیگه override نمی‌شه
- **Devirtualization** انجام می‌ده
- Direct Call → ~1 CPU cycle ✅

<div dir="ltr" style="text-align: left;">

```csharp
// JIT می‌تونه این کد:
Base obj = new Derived();
obj.Method();  // Virtual Call

// رو به این تبدیل کنه:
Derived obj = new Derived();
obj.Method();  // Direct Call (Optimized!)
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته پیشرفته:

Devirtualization فقط در **Release mode** و با **Tiered JIT** فعال انجام می‌شه. در Debug mode ممکنه همیشه Virtual Call بزنه.

### 📊 Performance Impact:

| حالت | Call Type | CPU Cycles | Optimization |
|------|-----------|------------|--------------|
| Virtual (بدون sealed) | Virtual Call | 2-3 | ❌ |
| Sealed Override | Direct Call | 1 | ✅ |
| Sealed Class | Direct Call | 1 | ✅ |

