<div dir="rtl" style="text-align: right;">

## ❓ Q17:

## Sealed Classes با Reflection و Runtime Type Information چطور کار می‌کنن؟

### ✅ جواب عمیق:

Reflection می‌تونه Sealed Classes رو در runtime بررسی کنه و Type Information رو بخونه.

### 🔍 Reflection برای Sealed Classes:

<div dir="ltr" style="text-align: left;">

```csharp
sealed class FinalClass {
    public void Method() { }
}

// Reflection:
Type type = typeof(FinalClass);
bool isSealed = type.IsSealed;  // True ✅
bool canInherit = !type.IsSealed;  // False

// بررسی Sealed Methods:
MethodInfo method = type.GetMethod("Method");
bool isSealedMethod = method.IsFinal;  // اگر sealed override باشه
```

<div dir="rtl" style="text-align: right;">

### 🔬 Type Information:

<div dir="ltr" style="text-align: left;">

```csharp
sealed class Dog : Animal {
    public sealed override void Speak() { }
}

Type type = typeof(Dog);

// بررسی Sealed Class:
Console.WriteLine(type.IsSealed);  // True ✅

// بررسی Sealed Methods:
MethodInfo[] methods = type.GetMethods();
foreach (var method in methods) {
    if (method.IsFinal) {
        Console.WriteLine($"Sealed: {method.Name}");  // Speak ✅
    }
}
```

<div dir="rtl" style="text-align: right;">

### 🎯 نکته پیشرفته:

Reflection می‌تونه **Sealed Information** رو بخونه و بررسی کنه که آیا کلاس یا متد sealed است یا نه.

### ⚠️ Performance:

Reflection برای Sealed Classes **خیلی کند** است (~1000-10000 CPU cycles). فقط برای **Inspection** استفاده کن.

